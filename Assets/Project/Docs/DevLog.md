# DevLog

## 2026-06-01

### 오늘 상황

오프라인 서점에서 《Do it! 유니티 6로 게임 제작 & 출시하기》를 구매하려고 했으나 재고가 없어 온라인 주문으로 변경했다. 책 학습은 도착 후 진행하기로 하고, 오늘은 프로젝트 시작을 위한 환경 구성과 기록 구조를 먼저 준비했다.

### 오늘 한 일

- 책 온라인 주문
- Unity 6 프로젝트 생성
- 프로젝트 기본 폴더 구조 생성
- GitHub 저장소 생성
- README 초안 작성
- DevLog 작성 시작
- 프로젝트 지침 작성 완료

### 오늘 정한 프로젝트 방향

오토배틀러는 1인 개발 범위에서 리소스와 시스템 부담이 크다고 판단하여, 2D 탑다운 액션 로그라이트 장르로 방향을 조정했다.

### 내일 할 일

- 첫 번째 기록 커밋 남기기
---
## 2026-06-02

## 오늘 목표
- Unity 6 엔진의 기본 인터페이스와 핵심 기능들을 빠르게 복습한다.
- 포트폴리오 개발에 들어가기 전 Unity 작업 환경과 기본 용어를 다시 정리한다.

### 오늘 한 일
- <<Do it! 유니티 6로 게임 제작 & 출시하기>>를 2-2까지 읽으며 Unity 기본 구조를 복습했다.
- Unity Hub, 프로젝트 실행, Scene View, Game View, Hierarchy, Inspector, Project 창의 역할을 확인했다.
- 2D GameObject와 3D GameObject의 기본 차이를 간단히 살펴보았다.
- GameObject와 Component의 관계를 다시 정리했다.
- URP와 HDRP의 차이를 간단하게 정리했다.
- Camera 설정이 Scene 화면과 Game 화면에 어떤 영향을 주는지 확인했다.
- 2D 프로젝트에서 사용할 수 있는 Light 2D 설정을 간단히 확인했다.
- 오늘은 기능 구현보다는 Unity 엔진에 다시 익숙해지는데 집중했다.

### 배운 내용
- Unity의 GameObject는 씬에 배치되는 기본 단위이며, 실제 기능은 Componet를 추가해 구성한다.
- Inspector는 선택한 오브젝트의 Transform, Sprite Renderer, Camera, Light 같은 컴포넌트 값을 확인하고 수정하는 핵심 창이다.
- Hierachy는 현재 Scene에 존재하는 오브젝트 구조를 확인하는 곳이고, Project창은 Assets 폴더 안의 파일과 리소스를 관리하는 곳이다.
- 2D GameObject는 Sprite 기반의 오브젝트를 다루는데 적합하고, 3D GameObject는 Mesh와 3D 공간을 기반으로 구성된다.
- Camera 설정은 실제 플레이어가 보게 되는 Game View에 직접적인 영향을 주기 때문에, 2D 탑다운 액션 로그라이트에서도 초반부터 기준을 잘 잡아야 한다.
- Light 2D는 2D 환경에서도 분위기와 가독성을 조절할 수 있는 기능이며, 추후 스테이지 연출이나 피격 효과 등에 응용할 수 있다.

### 포트폴리오에 적용할 부분
- Player, Enemy, Camera, UI를 각각 GameObject 단위로 구성하고, 기능은 Component와 Script로 분리하는 방향으로 설계한다.
- 앞으로 PlayerMovement, Health, EnemyAI, CameraFollow 같은 스크립트를 각각의 책임에 맞게 나누어 작성한다.
- 프로젝트 폴더 구조를 미리 정리해 Scripts, Prefabs, Scenes, Sprites, UI 리소스를 구분해서 관리한다.
- Camera는 플레이어 추적 기능을 구현할 때 중요한 기준 오브젝트가 되므로, 다음 작업에서 Camera 설정과 추적 방식을 함께 확인한다.
### 막힌 부분
- 없다.
### 해결한 방법
- 오늘은 모든 기능을 깊게 이해하려고 하기보다, Unity 작업 화면과 기본 용어를 빠르게 훑으며 전체 구조를 다시 잡는 방식으로 학습했다.
- 구현은 무리하게 진행하지 않고, 다음 작업에서 플레이어 오브젝트 생성과 이동 구현으로 이어가기로 했다.
### 내일 할 일
- Player 오브젝트를 생성한다.
- PlayerMovement 스크립트를 작성한다.
- 키보드 입력으로 플레이어 이동을 테스트한다.
- 가능하면 Camera가 Player를 따라가도록 간단한 CameraFollow 구조를 준비한다.
---
## 2026-06-03

### 오늘 목표
- 책 예제를 따라 URP 2D 프로젝트의 기본 설정을 확인한다.
- Tile Palette를 활용한 2D 맵 제작 흐름과 Tilemap 충돌 처리 방식을 학습한다.
- TMP를 활용해 화면에 스테이지 정보를 표시하는 방법을 확인한다.
### 오늘 한 일
- URP 2D 프로젝트를 생성하고 기본 게임 화면을 설정했다.
- Game View에서 해상도를 설정해 실제 게임 화면 기준을 확인했다.
- 외부 리소스를 가져와 이미지에 맞게 Pixel Per Unit 값을 조정했다.
- Tile Palette를 이용해 직접 타일을 배치하며 간단한 맵을 만들어보았다.
- Tilemap에 충돌 처리를 적용하는 방법을 학습했다.
- TextMeshPro를 활용해 현재 스테이지를 표시하는 텍스트 UI를 생성했다.
### 배운 내용
- Game View의 해상도 설정은 실제 플레이 화면 기준을 확인하는 데 중요하다.
- Pixel Per Unit 값은 이미지 리소스가 Unity 월드에서 어느 정도 크기로 보일지 결정하는 요소이므로, 리소스의 픽셀 크기에 맞게 조정해야 한다.
- Tile Palette를 사용하면 2D 맵을 타일 단위로 빠르게 배치할 수 있다.
- Tilemap에 Collider를 적용하면 플레이어가 벽이나 장애물 타일을 통과하지 못하도록 충돌 영역을 만들 수 있다.
- Composite Collider 2D를 함께 사용하면 연결된 타일들의 충돌 영역을 하나의 연속된 충돌 구조로 합성할 수 있다.
- 이를 통해 타일별로 나뉜 충돌 경계를 줄이고, 충돌 처리를 더 효율적으로 관리할 수 있다.
- TextMeshPro는 일반 텍스트보다 가독성과 표현력이 좋아 UI 텍스트를 구성할 때 유용하다.
### 포트폴리오에 적용할 부분
- 포트폴리오 프로젝트의 스테이지는 Tilemap 기반으로 구성할 수 있을 것 같다.
- 초반에는 직접 Tile Palette로 테스트 맵을 만들고, 이후에는 절차적 맵 생성 방식을 적용할 수 있는지 조사해볼 예정이다.
- 길찾기나 절차적 생성 알고리즘을 참고하여, 에디터에서 타일을 하나하나 찍지 않아도 일정한 규칙에 따라 맵을 생성하는 구조를 만들어보고 싶다.
- 예를 들어 방, 길, 벽 타일을 코드로 배치하고 Tilemap에 자동으로 그려주는 방식으로 확장할 수 있을 것 같다.
- 스테이지 번호나 현재 웨이브 정보는 TMP를 활용해 UI에 표시할 수 있을 것 같다.
### 막힌 부분
- 오늘은 컨디션 저하로 인해 플레이어 이동 구현까지는 진행하지 못했다.
- Tilemap, 리소스 설정, 충돌 처리, TMP UI 등 새로 확인할 내용이 많아 구현 범위를 줄였다.
### 해결한 방법
- 오늘은 무리해서 플레이어 이동까지 진행하지 않고, 2D 맵 구성과 UI 표시 흐름을 학습하는 데 집중했다.
- 대신 포트폴리오에 적용할 수 있는 아이디어를 정리해두어 이후 작업으로 이어갈 수 있도록 했다.
### 내일 할 일
- 플레이어 오브젝트를 생성한다.
- Rigidbody2D와 Collider2D를 추가해 물리 기반 이동을 준비한다.
- PlayerMovement 스크립트를 작성하고 키보드 입력으로 이동을 테스트한다.
---
## 2026-06-04

### 오늘 목표
- 플레이어 오브젝트 생성
- Rigidbody2D 기반 물리 이동 구현
- 플레이어 이동에 맞는 애니메이션 적용
- 카메라와 조명이 플레이어를 따라오도록 설정
### 오늘 한 일
- Player 오브젝트를 생성하고 필요한 컴포넌트를 추가했다.
- Rigidbody2D를 활용해 플레이어가 물리 기반으로 이동하도록 구현했다.
- 입력값에 따라 플레이어가 이동하는 방향을 처리했다.
- Blend Tree를 활용해 플레이어 이동 애니메이션을 적용했다.
- 카메라가 플레이어를 따라오도록 설정했다.
- 조명이 플레이어를 따라다니도록 설정했다.
- 각 기능을 작은 단위로 나누어 적용하고, Play 모드에서 이동, 애니메이션, 카메라 추적, 조명 추적이 정상적으로 동작하는지 확인했다.
### 배운 내용
- Rigidbody2D를 사용하면 물리 시스템을 기반으로 오브젝트를 이동시킬 수 있다.
- 플레이어 이동은 입력 처리와 실제 이동 처리를 분리해서 관리하는 것이 좋다.
- Blend Tree를 사용하면 이동 방향에 따라 자연스럽게 애니메이션을 전환할 수 있다.
- 카메라와 조명을 플레이어 기준으로 따라오게 하면 탑다운 액션 게임의 기본 플레이 화면을 구성할 수 있다.
### 막힌 부분
- 없음
### 해결한 방법
- 없음
### 내일 할 일
- 적 오브젝트 생성 학습
- 오브젝트별 스탯 구현 학습
- 체력 UI 구현 학습
---
## 2026-06-05

### 오늘 목표
- Enemy 오브젝트 생성 방법 학습
- Tilemap 범위 기반 Enemy 스폰 위치 설정 학습
- Player와 Enemy의 공통 스탯 구조 학습
- Player와 Enemy를 따라다니는 체력바 UI 구현 방식 학습
### 오늘 한 일
- Enemy 오브젝트를 생성하고 스폰 위치를 설정하는 방법을 학습했다.
- Tilemap의 Bounds 크기를 기준으로 맵 내부 좌표를 탐색하고, 그중 스폰 가능한 위치만 선별하여 리스트에 저장했다.
- 저장된 스폰 가능 위치 리스트에서 Random.Range를 사용해 임의의 인덱스를 선택하고, 해당 위치에 Enemy가 생성되도록 구성했다.
- Player와 Enemy가 공통으로 사용할 기본 스탯을 EntityState 구조체로 정의했다.
- 공통 스탯과 기본 동작을 관리하기 위해 EntityBase 클래스를 만들고, Player와 Enemy가 이를 상속받는 구조를 학습했다.
- UI Image를 활용해 Player와 Enemy를 따라다니는 체력바 UI를 만드는 방법을 학습했다.
### 배운 내용
- Tilemap의 Bounds 정보를 활용하면 맵 범위 안에서 오브젝트를 생성할 수 있는 후보 위치를 계산할 수 있다.
- 스폰 가능한 위치를 미리 리스트로 저장해두면, Enemy 생성 시 매번 위치를 새로 계산하지 않고 랜덤 인덱스만 선택해서 사용할 수 있다.
- Player와 Enemy처럼 공통 스탯이 필요한 객체는 별도의 구조체와 베이스 클래스를 통해 중복 코드를 줄일 수 있다.
- EntityBase 같은 부모 클래스를 두면 체력, 이동속도, 공격력 같은 공통 데이터를 한 곳에서 관리할 수 있다.
- 월드 공간에 존재하는 캐릭터 위에 UI 체력바를 표시하려면, 대상의 위치를 기준으로 UI 위치를 갱신해야 한다.
### 막힌 부분
- Tilemap의 Bounds를 기준으로 스폰 가능한 위치를 선별하는 흐름이 처음에는 복잡하게 느껴졌다.
- Player와 Enemy의 기본 스탯을 각각 따로 만들지 않고 공통 구조로 묶는 방식이 아직 완전히 익숙하지 않았다.
- 체력바 UI가 실제 캐릭터를 따라다니도록 연결하는 좌표 처리 방식이 헷갈릴 수 있다고 느꼈다.
### 해결한 방법
- Tilemap 범위 안의 좌표를 순회하면서 스폰 가능한 위치만 리스트에 저장하는 방식으로 흐름을 나누어 이해했다.
- EntityState 구조체에는 기본 스탯 데이터를 두고, EntityBase 클래스에는 Player와 Enemy가 공통으로 사용할 기능을 두는 방식으로 역할을 구분했다.
- 체력바 UI는 대상 오브젝트의 위치를 기준으로 계속 갱신되도록 만들어, 캐릭터를 따라다니는 방식으로 이해했다.
### 내일 할 일
- Enemy가 Player를 대상으로 기본 공격을 수행하는 흐름 학습
- Player의 공격 대상 탐색 및 목표 설정 방식 학습
- 공통 스탯 관리를 위한 스탯 클래스 구조 학습
- 기본 공격 스킬을 생성하고 캐릭터의 공격 로직과 연결하는 방법 학습
---
## 2026-06-06

### 오늘 목표
- 적 기본 공격 구현 방법 학습
### 오늘 한 일
- 적이 일정한 공격 쿨타임 간격으로 총알을 발사해 플레이어를 공격하도록 구현했다.
- 총알의 충돌 판정은 OnTriggerEnter2D를 사용해 처리했다.
- 충돌한 대상의 태그가 Player인지 확인한 뒤, TryGetComponent로 EntityBase 컴포넌트를 가지고 있는지 검사했다.
- 조건을 만족하는 경우 TakeDamage를 호출해 플레이어에게 데미지가 적용되도록 만들었다.
### 배운 내용
- OnTriggerEnter2D를 활용하면 총알처럼 충돌 시 특정 동작을 실행해야 하는 오브젝트의 판정을 처리할 수 있다는 것을 학습했다.
- 단순히 태그만 검사하는 것이 아니라, TryGetComponent를 함께 사용하면 대상이 실제로 데미지를 받을 수 있는 구조인지 한 번 더 확인할 수 있다는 점을 배웠다.
- enum으로 정의한 타입을 .ToString()으로 문자열화하여 코루틴 제어에 활용하는 방식이 인상 깊었다.
- 적 공격, 총알 충돌, 데미지 적용 흐름을 분리하면 이후 플레이어 공격이나 스킬 시스템으로 확장하기 쉬운 구조가 된다는 점을 확인했다.
### 막힌 부분
- 특별히 크게 막힌 부분은 없었지만, 충돌한 대상이 플레이어인지 확인한 뒤 데미지를 적용하는 흐름을 정확히 이해하는 것이 중요했다.
### 해결한 방법
- OnTriggerEnter2D에서 충돌한 Collider2D의 태그를 먼저 검사하고, 이후 TryGetComponent<EntityBase>를 통해 데미지를 받을 수 있는 대상인지 확인하는 방식으로 처리했다.
- 조건을 모두 만족했을 때만 TakeDamage를 실행하도록 하여 불필요한 오류 가능성을 줄였다.
### 내일 할 일
- 플레이어 공격 목표 설정 학습
- 스탯 클래스 설정 학습
- 기본 공격 스킬 생성 학습
---
## 2026-06-07

### 오늘 목표
- 플레이어가 목표를 설정하는 방법을 학습
- 지금까지 학습한 내용을 바탕으로 포트폴리오 프로젝트의 기초 기능 구현을 시작
- 테스트용 맵을 만들고, 플레이어가 이동할 수 있는 기본 구조를 구현
### 오늘 한 일
- 프로젝트에서 사용할 기본 리소스를 추가했다.
- 기능 테스트를 위한 타일맵을 배치했다.
- 타일맵 충돌 구조를 설정해 플레이어가 벽을 통과하지 않도록 만들었다.
- 플레이어 오브젝트를 생성하고 Rigidbody2D, Collider2D 등 기본 컴포넌트를 설정했다.
- Rigidbody2D를 활용해 플레이어 이동 처리를 구현했다.
- 추후 UI 구현을 위해 TextMeshPro 필수 리소스를 프로젝트에 추가했다.
### 배운 내용
- 플레이어의 공격 목표 설정 방식을 학습했다.
- EnemySpawner가 생성한 적들을 EntityBase 타입의 리스트로 관리하고, 플레이어가 해당 리스트를 순회하면서 가장 가까운 적을 찾는 구조를 이해했다.
- 거리 비교에는 sqrMagnitude를 사용할 수 있으며, 실제 거리값이 필요하지 않은 경우 제곱근 계산을 줄일 수 있다는 점을 배웠다.
- 현재 공격 대상은 target으로 저장하고, targetMask를 통해 어떤 적을 타겟팅 중인지 시각적으로 표시할 수 있다는 것을 학습했다.
- 타겟을 바라보는 회전 처리는 Utils 같은 정적 유틸 클래스에 분리할 수 있으며, Mathf.Atan2()와 Quaternion.Euler()를 사용해 방향 회전값을 만들 수 있다는 것을 배웠다.
### 막힌 부분
- 없음
### 해결한 방법
- 없음
### 내일 할 일
- 플레이어 이동 애니메이션을 학습하고 적용한다.
- 플레이어 이동 시 재생할 이펙트를 만들어본다.
- 플레이어와 적의 데이터를 관리할 스탯 클래스 구조를 학습한다.
---
## 2026-06-08

### 오늘 목표
- 플레이어 이동에 시각적 피드백을 추가하고, 카메라와 조명이 플레이어를 추적하도록 구현
### 오늘 한 일
- 플레이어 이동 상태에 따라 파티클 이펙트가 재생되도록 연결했다.
- Particle System의 Play On Awake 옵션을 비활성화하고, 스크립트에서 이동 여부에 따라 재생을 제어했다.
- 카메라가 플레이어를 따라오도록 설정했다.
- 2D Spot Light가 플레이어를 따라오도록 설정했다.
### 배운 내용
- Play On Awake는 자동 재생 여부만 제어하며, 스크립트에서 Play()를 호출하면 파티클을 직접 재생할 수 있다는 점을 확인했다.
- 파티클 이펙트는 매 프레임 Play()를 호출하기보다, 재생 상태를 확인한 뒤 필요한 순간에만 실행하는 방식이 안정적이라는 점을 배웠다.
- 플레이어 이동, 카메라 추적, 조명 추적처럼 시각적 피드백이 연결되면 조작감이 더 명확해진다는 점을 확인했다.
### 막힌 부분
- 스케쥴 조절 실패로 전날 계획했던 학습 내용은 진행하지못하였다.
- Play On Awake를 비활성화한 상태에서 스크립트로 파티클을 재생했을 때 이펙트가 보이지 않는 문제가 있었다.
### 해결한 방법
- Play On Awake 옵션의 역할을 확인하고, ParticleSystem.Play() 호출 방식과 Emission, Renderer, 재생 상태 조건을 점검했다.
- 이동 중일 때만 파티클이 재생되도록 조건을 나누어 처리했다.
- 학습과 기능개발의 스케쥴을 조금 느슨하게 잡아서 주 3일은 학습을 진행하고 그 외의 시간은 기능개발에 집중할 수 있도록 계획을 수정했다.
### 내일 할 일
- 적 오브젝트를 실제 포트폴리오 프로젝트에 생성하고 기본 배치 상태를 확인한다.
- 플레이어와 적이 공통으로 사용할 수 있는 스탯 구조를 설계하고, 체력/이동속도/공격력 등의 데이터를 분리한다.
- 플레이어가 일정 범위 내의 적을 탐색하고 타겟팅할 수 있는 기능을 적용한다.
---
## 2026-06-09

### 오늘 목표
- 적 오브젝트를 생성하고 플레이어와 적의 기본 스탯 구조를 구현
### 오늘 한 일
- 테스트용 Enemy 오브젝트를 생성했다.
- Player와 Enemy가 공통으로 사용할 수 있는 EntityStats 구조를 만들었다.
- EntityBase를 통해 체력, 이동속도, 공격력 등 기본 데이터를 관리하는 방향을 잡았다.
- 플레이어가 씬에 존재하는 적을 탐색하고 가장 가까운 적을 타겟으로 저장하는 기본 흐름을 구현했다.
### 배운 내용
- 플레이어와 적이 공통으로 가지는 데이터는 별도 구조로 분리하는 것이 유지보수에 유리하다는 점을 확인했다.
- 타겟팅 기능은 공격 기능과 분리해서 먼저 검증하는 것이 안정적이라는 점을 확인했다.
### 막힌 부분
- 없음
### 해결한 방법
- 없음
### 내일 할 일
- 플레이어와 적을 따라다니는 체력바 HUD 구현한다.
- 적 기본 공격 기능을 구현한다.
---
## 2026-06-10

### 오늘 목표
- 플레이어와 적을 따라다니는 체력바 HUD 구현
- 적 기본 공격을 구현
### 오늘 한 일
- 플레이어와 적의 체력 상태를 표시하는 체력바 HUD를 구현하였다.
- 체력바가 대상 오브젝트를 따라다니도록 연결하였다.
- 적이 플레이어를 대상으로 총알을 발사하는 기본 공격 패턴을 구현하였다.
- 적의 총알이 플레이어에게 충돌했을 때 피격 처리가 발생하도록 구현하였다.
- 플레이어 피격 시 데미지가 적용되고 체력 변화가 HUD에 반영되도록 전투 흐름을 연결하였다.
### 배운 내용
- 체력 데이터를 HUD로 표시하기 위해 대상 오브젝트와 UI를 연결하는 구조가 필요하다는 것을 확인했다.
- 적의 공격은 발사 로직, 투사체 이동, 충돌 판정, 데미지 처리로 나누어 생각할 수 있다는 것을 배웠다.
- 피격 처리와 체력 HUD 갱신이 연결되면서, 전투 데이터와 UI 반영 흐름을 명확히 관리해야 한다는 점을 확인했다.
### 막힌 부분
- 없음
### 해결한 방법
- 없음
### 내일 할 일
- 플레이어와 적의 데이터를 유연하게 관리하기 위한 스탯 클래스 구조를 학습한다.
- 플레이어 기본 공격 스킬을 구현하기 위한 구조와 동작 흐름을 학습한다.
- 학습한 내용을 프로젝트에 적용할 수 있는 방식으로 정리한다.
---
## 2026-06-11

### 오늘 목표
- 플레이어와 적의 데이터를 유연하게 관리하기 위한 스탯 클래스 구조 학습
- 플레이어 기본 공격 스킬 구현을 위한 구조와 동작 흐름 학습
- 어떤 방식으로 내 프로젝트에 적용할지 고민해보고 정리
### 오늘 한 일
- Stat 클래스를 통해 스탯 하나의 값과 기능을 분리해서 관리하는 방법을 학습했다.
- EntityStats 구조를 통해 여러 Stat을 배열로 묶어 캐릭터의 능력치로 관리하는 방법을 확인했다.
- EntityBase에서 현재 체력 초기화와 데미지 처리 흐름을 확인했다.
- SkillSystem, SkillGad, ProjectileGad를 통해 기본 공격이 실행되는 흐름을 학습했다.
- 피격 이펙트 자동 삭제와 데미지 텍스트 표시 구조를 확인했다.
### 배운 내용
- Stat 클래스는 단순한 숫자 변수가 아니라, 기본값, 보너스값, 최소값, 최대값을 함께 관리하는 스탯 전용 클래스다.
- Value는 직접 저장되는 값이 아니라 defaultValue와 bonusValue를 더한 뒤 Clamp로 범위를 제한해 계산되는 읽기 전용 값이다.
- DefaultValue가 변경되면 이전 값과 현재 값을 비교하고, 값이 실제로 변경되었을 때 이벤트를 호출할 수 있다.
- EntityStats는 여러 Stat을 배열로 가지고 있으며, FirstOrDefault를 사용해 StatType이 일치하는 첫 번째 Stat을 검색할 수 있다.
- CurrentHP는 실제 전투 중 변하는 현재 체력이고, StatType.HP로 찾는 HP Stat은 최대 체력 기준값으로 사용할 수 있다.
- EntityBase의 Setup은 HP Stat의 Value를 CurrentHP에 대입해 현재 체력을 초기화한다.
- TakeDamage는 CurrentHP.DefaultValue를 감소시키며, 이 과정에서 Stat의 Clamp 처리와 값 변경 이벤트가 함께 동작할 수 있다.
- SkillSystem은 공격 가능 여부를 검사하고, SkillGad는 쿨타임과 데미지 계산을 담당한다.
- ProjectileGad는 발사체를 목표 방향으로 이동시키고, 원래 타겟과 충돌했을 때 데미지 처리, 피격 이펙트, 데미지 텍스트 생성을 수행한다.
- ParticleAutoDestroyer는 재생이 끝난 파티클 오브젝트를 자동으로 삭제하여 불필요한 오브젝트가 남지 않도록 한다.
- UIDamageText는 데미지 숫자를 표시하고 위쪽으로 이동하면서 점점 투명해진 뒤 삭제되는 방식으로 피격 피드백을 제공한다.
### 막힌 부분
- Stat 클래스의 프로퍼티 문법과 이벤트 호출 흐름이 처음에는 직관적으로 이해되지 않았다.
- EntityStats에서 FirstOrDefault를 사용해 StatType 기준으로 스탯을 검색하는 구조가 헷갈렸다.
- CurrentHP와 StatType.HP로 찾는 HP Stat의 역할 차이를 구분하는 데 시간이 걸렸다.
- 기본 공격 흐름에서 SkillSystem, SkillGad, ProjectileGad가 각각 어떤 책임을 가지는지 연결해서 이해하는 데 시간이 걸렸다.
### 해결한 방법
- Stat은 스탯 하나를 관리하고, EntityStats는 여러 Stat을 묶으며, EntityBase는 실제 전투 동작을 처리하는 구조로 역할을 나누어 정리했다.
- FirstOrDefault는 stats 배열을 순회하면서 StatType이 일치하는 첫 번째 Stat을 반환하고, 없으면 null을 반환한다는 방식으로 이해했다.
- HP Stat은 최대 체력 기준값, CurrentHP는 전투 중 변화하는 현재 체력으로 구분해서 정리했다.
- 기본 공격 흐름을 SkillSystem → SkillGad → ProjectileGad → EntityBase.TakeDamage 순서로 나누어 이해했다.
- 현재 코드 파악은 대부분 완료했지만, 구조가 익숙해지기까지는 실제 구현에 반복 적용하는 과정이 필요하다고 판단했다.
### 내일 할 일
- 버프 스킬을 추가하는 방법을 학습한다.
- Stat의 BonusValue를 활용해 버프가 스탯에 영향을 주는 흐름을 확인한다.
- 버프 적용, 지속 시간, 해제 처리 구조를 어떻게 분리할 수 있는지 정리한다.
---
## 2026-06-12

### 오늘 목표
- 버프 스킬의 데이터 형태로 저장하는 방법과 저장한 데이터를 불러와 사용하고 관리하는 방법을 학습
- Stat의 BonusValue를 활용해 버프 스킬이 스탯에 영향을 주는 흐름에 대해서 학습
- 버프 스킬의 구조를 어떻게 분리하는지 이해하고 정리
### 오늘 한 일
- 책 예제를 바탕으로 버프 스킬 관련 구조를 살펴보았다.
- SkillTemplete, SkillBase, SkillBuff, SkillSystem의 역할을 대략적으로 확인했다.
- 버프 스킬이 스탯 보너스와 연결되는 구조를 학습했지만, 아직 전체 흐름을 완벽하게 이해하지 못했다.
### 배운 내용
- 버프 스킬은 스탯에 보너스 값을 적용하는 방식으로 동작할 수 있다.
- 스탯 클래스는 기본값, 보너스값, 최종값 계산은 공통으로 관리하기 위해 사용된다.
- DefaultValue, BonusValus, Value의 차이를 다시 정리할 필요가 있다.
### 막힌 부분
- 피로로 인해 대부분의 버프 스킬의 실제 적용 흐름을 이해하는데 어려움이 있었다.
- Stat 클래스의 구조가 다시 헷갈리기 시작했다.
### 해결한 방법
- 오늘 진행한 학습을 내일 복습하면서 내용을 정리할것이다.
### 내일 할 일
- Stat 클래스의 역할을 다시 한번 복습
- 버프 스킬 코드 흐름 다시 정리
- 스킬 시스템의 구조 확인
---
## 2026-06-13

### 오늘 목표
- Stat 클래스와 스킬 시스템 예제 코드의 전체 흐름 복습
- 코드 흐름을 따라가며 헷갈렸던 개념 정리
### 오늘 한 일
- Stat 클래스의 역할을 다시 상기했다.
    - DefaultValue, BonusValue, Value의 역할 차이를 복습했다.
    - Value가 단순 저장값이 아니라, 기본값과 보정값을 바탕으로 계산되는 최종값이라는 점을 다시 확인했다.
    - HP는 최대 체력의 기준이 되는 스탯임을 다시 한번 확인했다.
    - CurrentHP는 인게임에서 사용되는 현재 체력 상태값이다.
- 스킬 시스템의 전체 동작 흐름을 코드 기준으로 파악했다.
    - SkillTemplate은 스킬 데이터를 저장하는 역할을 한다는 점을 정리했다.
    - buffStatList를 통해 스킬이 영향을 줄 스탯만 선택적으로 관리할 수 있다는 점을 확인했다.
    - SkillBuff.OnLevelUp()에서 스킬 데이터에 저장된 증가량을 owner의 실제 Stat의 BonusValue에 적용하는 흐름을 확인했다.
- SkillSystem, SkillGad, ProjectileGad의 주요 코드 흐름을 살펴봤다.
    - SkillSystem의 skills는 습득 가능하거나 강화 가능한 전체 스킬의 런타임 객체를 관리하는 Dictionary로 이해했다.
    - StartCoroutine(nameof(SpawnProjectile))에서 nameof가 메서드 이름을 문자열로 변환하는 역할임을 확인했다.
    - LayerMask.NameToLayer("Enemy")와 비트 시프트를 통해 Enemy 레이어만 검사하는 LayerMask를 만드는 방식을 정리했다.
### 배운 내용
- Stat 클래스는 능력치를 단순한 숫자로 관리하는 것이 아니라, 기본값과 보정값을 분리하여 최종값을 계산하기 위한 구조다.
- Value는 DefaultValue와 BonusValue가 반영된 최종값이므로, 버프나 성장으로 변할 수 있는 능력치의 기준값으로 사용된다.
- CurrentHP와 HP는 모두 체력과 관련되어 있지만 성격이 다르다.
    - CurrentHP는 현재 남아있는 체력 상태값이다.
    - HP는 현재 체력이 넘지 말아야 할 최대 기준값이다.
- SkillTemplate은 스킬의 고정 데이터를 저장하고, SkillBuff는 해당 데이터를 바탕으로 owner의 실제 스탯에 보정값을 적용한다.
- 스킬 시스템은 캐릭터의 값을 직접 무작정 바꾸는 것이 아니라, Stat 구조를 통해 능력치에 영향을 주는 방식으로 동작한다.
- LayerMask는 레이어 번호 자체가 아니라, 특정 레이어를 검사하기 위한 비트마스크 값으로 사용된다.
### 막힌 부분
- Stat 구조에서 DefaultValue와 Value가 각각 어떤 상황에서 사용되는지 헷갈렸다.
- SkillBuff.OnLevelUp()에서 buffStatList의 Stat 값이 실제로 어느 대상의 BonusValue에 적용되는지 헷갈렸다.
- SkillSystem의 skills가 습득한 스킬 목록인지, 전체 스킬 목록인지 구분이 필요했다.
- nameof와 LayerMask 비트 시프트처럼 C# 문법과 Unity API가 함께 사용된 부분의 의미를 확인할 필요가 있었다.
### 해결한 방법
- Stat의 역할을 먼저 큰 틀에서 다시 정리한 뒤, DefaultValue, BonusValue, Value의 관계를 순서대로 복습했다.
- SkillTemplate, SkillBuff, SkillSystem의 역할을 분리해서 정리했다.
    - SkillTemplate은 스킬 데이터 원본이다.
    - SkillBuff는 스킬 데이터에 저장된 값을 owner의 Stat.BonusValue에 적용한다.
    - SkillSystem은 스킬 런타임 객체와 레벨 상태를 관리한다.
- nameof는 문자열 기반 코루틴 호출에서 메서드 이름을 안전하게 가져오기 위한 방식으로 이해했다.
- LayerMask는 특정 레이어 번호를 비트마스크로 변환하여 Physics2D 검색 필터로 사용하는 구조로 정리했다.
### 내일 할 일
- 프로젝트에 적용해 보기 위한 Stat 클래스 구현
---
## 2026-06-14

### 오늘 목표
- Stat 클래스를 포트폴리오 프로젝트의 체력 구조에 적용
### 오늘 한 일
- 예제의 Stat 구조를 프로젝트에 적용했다.
- DefaultValue, BonusValue, Value를 분리해 스탯 값을 관리하도록 했다.
- 데미지 처리 시 CurrentHP.DefaultValue를 수정하고, UI에서는 CurrentHP.Value를 읽어 체력바를 갱신하는 흐름을 확인했다.
### 배운 내용
- DefaultValue는 기본 스탯값, BonusValue는 버프나 스킬, 아이템으로 추가되는 보정값이다.
- Value는 DefaultValue와 BonusValue를 더한 뒤 Clamp가 적용된 최종 계산값이다.
- Value는 읽기 전용이므로 값을 변경할 때는 DefaultValue나 BonusValue를 수정해야 한다.
- CurrentHP는 인게임에서 계속 변하는 현재 체력의 상태값이므로, 현재 구조에서는 DefaultValue를 현재 체력 저장값처럼 사용한다.
### 막힌 부분
- BonusValue를 사용하지 않는 CurrentHP에서는 Value와 DefaultValue 두 값의 결과 차이가 없어서 값을 읽어야 할 때 둘 중 뭘 써야 할지 고민했다.
- 예제 코드와 구조가 거의 같아 포트폴리오로서의 차별성이 부족하지 않을까 고민했다.
### 해결한 방법
- Value는 읽기 전용 속성이기 때문에 값을 읽어야 한다면 Value를 사용하고 값을 수정해야 할 때는 DefaultValue를 사용하기로 정리했다.
- 현재 구현은 학습 기반의 1차 적용으로 보고, 추후 스킬/버프 시스템과 연결하면서 포트폴리오에 맞게 개선하기로 했다.
### 내일 할 일
- 기본 공격 스킬 구현
--- 
## 2026-06-15

### 오늘 목표
- 플레이어 기본 공격 스킬 구현
### 오늘 한 일
- 예제 코드를 프로젝트에 직접 적용하며 기본 공격 스킬의 동작 흐름을 확인했다.
- ProjectileGad를 통해 기본 공격 발사체의 이동과 충돌 처리를 구현했다.
- SkillGad를 통해 기본 공격의 발사 주기, 데미지 계산, 발사체 생성 흐름을 적용했다.
- SkillSystem을 통해 스킬을 관리하고 실행하는 구조를 확인했다.
- 플레이어가 일정 시간마다 발사체를 발사하고, 적에게 적중하면 TakeDamage를 통해 데미지를 주도록 구현했다.
### 배운 내용
- 기본 공격 기능은 발사체 제어, 공격 주기 관리, 데미지 처리, 스킬 관리 역할로 나누어 구현할 수 있다.
- 발사체는 직접 데미지를 계산하기보다, 충돌한 대상의 TakeDamage를 호출해 기존 데미지 처리 흐름을 재사용하는 방식이 좋다.
- SkillSystem은 개별 스킬을 직접 구현하는 클래스가 아니라, 스킬들을 보관하고 실행 흐름을 관리하는 역할을 가진다.
- 예제 코드를 프로젝트에 적용하면서 각 클래스가 어떤 책임을 가지는지 흐름을 따라가며 익숙해졌다.
### 막힌 부분
- 스킬 구조가 아직 완전히 익숙하지 않아, 각 클래스의 역할과 호출 흐름을 반복해서 확인할 필요가 있었다.
### 해결한 방법
- 예제 코드를 그대로 읽는 것에서 끝내지 않고, 프로젝트에 직접 적용하면서 ProjectileGad, SkillGad, SkillSystem의 역할을 나누어 확인했다.
### 내일 할 일
- 기본 공격 피격 이펙트 구현
- 데미지 텍스트 출력 구현
---
## 2026-06-16

### 오늘 목표
- 기본 공격 적중 시 피격 이펙트와 데미지 텍스트를 출력
### 오늘 한 일
- 기본 공격 발사체가 적에게 적중했을 때 피격 파티클을 생성하도록 구현했다.
- 데미지 발생 시 데미지 텍스트를 생성해 공격 결과를 화면에 표시하도록 했다.
- 크리티컬 공격이 발생했을 경우 데미지 텍스트 색상을 빨간색으로 표시해 일반 공격과 구분되도록 했다.
- 파티클 재생이 끝나면 자동으로 오브젝트를 제거하는 ParticleAutoDestroyer를 추가했다.
- 데미지 텍스트가 일정 시간 동안 이동하며 서서히 사라지도록 UIDamageText를 추가했다.
- 기존 ProjectileGad와 SkillGad를 수정해 데미지 값과 크리티컬 여부를 발사체에 전달하도록 연결했다.
### 배운 내용
- 전투 피드백은 데미지 처리 로직과 분리하되, 충돌 판정이 발생하는 지점에서 함께 호출하면 흐름을 직관적으로 관리할 수 있다.
- 데미지 텍스트 색상 변경만으로도 크리티컬 여부를 플레이어에게 명확하게 전달할 수 있다.
- 파티클이나 UI처럼 짧게 생성되고 사라지는 오브젝트는 자동 제거 처리를 붙여두면 씬에 불필요한 오브젝트가 남는 것을 방지할 수 있다.
### 막힌 부분
- 오늘 작업은 이펙트와 UI 출력 중심이었고, 구현 과정에서 크게 막힌 부분은 없었다. 
### 내일 할 일
- 스킬 시스템 구조를 프로젝트에 적용한다.
- 버프 스킬을 구현하고 스탯 보정값에 반영되는 흐름을 확인한다.
- 작업 과정에서 확인한 SkillTemplate, SkillBase, SkillBuff, Stat의 연결 구조를 정리한다.
---
## 2026-06-17

### 오늘 목표
- 스킬 시스템을 확장하고, 버프 스킬이 플레이어 스탯에 반영되는 흐름을 구현한다.
### 오늘 한 일
- 스킬의 고정 데이터를 관리하기 위한 SkillTemplate ScriptableObject 구조를 추가했다.
- 모든 스킬이 공통으로 사용할 부모 클래스 SkillBase를 추가했다.
- 버프 스킬 전용 클래스 SkillBuff를 추가하고, 레벨업 시 지정된 스탯의 BonusValue가 증가하도록 구현했다.
- SkillSystem에서 Resources/Skills 폴더의 스킬 데이터를 로드하고, 스킬 타입에 따라 스킬 인스턴스를 생성하도록 구현했다.
- 플레이어가 보유한 스킬 정보를 Dictionary로 관리하고, 임시 입력을 통해 랜덤 스킬을 선택해 레벨업하는 테스트 흐름을 구현했다.
- 기본 공격 스킬에서 CooldownTime, ProjectileCount, Damage, CriticalChance, CriticalMultiplier 등의 스탯을 참조해 공격 성능이 결정되도록 구현했다.
- 발사체가 적에게 명중했을 때 데미지를 적용하고, 추가 효과를 처리하는 구조를 구현했다.
- 플레이어의 체력 회복 기능을 구현했다.
- Evasion 스탯을 활용해 피격 시 회피 판정을 적용했다.
### 배운 내용
- SkillTemplate을 사용하면 스킬의 고정 데이터를 코드와 분리하여 관리할 수 있어, 새로운 스킬을 추가하거나 값을 조정하기 쉬워진다.
- SkillBase는 스킬의 공통 정보와 레벨업 흐름을 담당하고, 실제 효과 적용은 하위 클래스에서 처리하도록 분리할 수 있다.
- SkillBuff는 버프 스킬의 효과를 담당하며, 스탯의 DefaultValue를 직접 수정하지 않고 BonusValue를 증가시켜 최종 계산값인 Value에 영향을 주는 구조로 동작한다.
- SkillSystem은 스킬 데이터 로드, 스킬 인스턴스 생성, 보유 스킬 관리, 레벨업 처리를 담당하는 중심 시스템 역할을 한다.
- 기본 공격의 CooldownTime은 스킬 재사용 주기이고, attackRate는 여러 발의 발사체를 발사할 때 사용하는 발사체 간격이라는 점을 확인했다.
- 스킬 시스템을 데이터, 공통 흐름, 실제 효과, 관리 시스템으로 나누면 이후 공격 스킬이나 패시브 스킬을 추가할 때 확장하기 쉽다.
### 막힌 부분
- 없음.
### 내일 할 일
- 주 공격 스킬의 구현 방식을 학습한다.
---
## 2026-06-18

### 오늘 목표
- 주 공격 스킬별 Projectile 생성 구조 학습
- ProjectileBase를 기준으로 발사체 타입을 통일하는 방식 확인
### 오늘 한 일
 주 공격 스킬이 각 공격 방식에 맞는 Projectile 프리팹을 생성하는 구조를 확인했다.
- 공격 스킬 데이터가 SkillTemplate에서 관리되는 흐름을 확인했다.
  - attackBaseStats
  - attackBuffStats
  - projectile
- SkillBase에서 공격 스킬 공통 데이터를 초기화하는 흐름을 확인했다.
- SkillEmission에서 쿨타임, 발사체 개수, 공격 주기에 따라 Projectile을 생성하는 구조를 확인했다.
- ProjectileBase를 부모 클래스로 두고, 각 Projectile이 이를 상속받아 공통 타입으로 관리되는 구조를 확인했다.
- Projectile 종류별 동작 차이를 확인했다.
  - ProjectileStraight: 목표 방향으로 직선 이동
  - ProjectileHoming: 목표를 추적하며 이동
  - ProjectileLine: LineRenderer를 활용한 선형 공격
  - ProjectileCubicHoming: CubicCurve를 활용한 곡선 추적 공격
- ProjectileCollision2D에서 적 충돌, 데미지 적용, 피격 이펙트, 데미지 텍스트 생성, 충돌 후 제거 처리를 담당하는 것을 확인했다.
- Utils에서 발사체 회전, 각도 계산, 곡선 이동에 필요한 보조 메서드를 사용하는 것을 확인했다.
### 배운 내용
- 주 공격 스킬이 직접 모든 발사체 동작을 처리하는 것이 아니라, Projectile 프리팹을 생성하고 실제 이동 방식은 각 Projectile 클래스가 담당하도록 분리할 수 있다.
- ProjectileBase를 상속 구조의 기준으로 두면, 서로 다른 발사체도 공통 타입으로 다룰 수 있다.
- 공통 초기화는 ProjectileBase에서 처리하고, 직선 이동, 추적 이동, 선형 공격, 곡선 이동 같은 개별 동작은 자식 클래스에서 처리할 수 있다.
- SkillTemplate은 스킬의 고정 데이터와 Projectile 프리팹 정보를 가지고, SkillEmission은 해당 데이터를 바탕으로 실제 발사체를 생성하는 역할을 한다.
### 막힌 부분
- 주 공격 스킬, SkillEmission, ProjectileBase, 개별 Projectile 클래스 사이의 책임 분리가 아직 명확하게 정리되지 않았다.
- ProjectileBase를 기준으로 타입을 통일하는 이유는 이해했지만, 실제 코드 흐름을 말로 설명하는 데는 아직 정리가 필요하다.
- CubicCurve를 활용한 ProjectileCubicHoming의 이동 방식은 추가 복습이 필요하다.
### 해결한 방법
- 오늘은 전체 구조를 완전히 정리하기보다, 예제 스크립트에서 어떤 클래스가 어떤 역할을 하는지 확인하는 데 집중했다.
- 개념 정리는 내일 다시 진행하기로 했다.
### 내일 할 일
- 주 공격 스킬 구조를 다시 복습한다.
- SkillTemplate → SkillBase → SkillEmission → ProjectileBase → 개별 Projectile → ProjectileCollision2D 순서로 흐름을 정리한다.
- ProjectileBase를 사용하는 이유와 각 Projectile 자식 클래스의 역할을 말로 설명할 수 있게 정리한다.
---
## 2026-06-19

### 오늘 목표
- 어제 학습한 스킬 발사체 예제 코드의 전체 흐름 이해
- SkillBase, ProjectileBase, ProjectileCollision2D의 역할 구분
- 발사체 이동과 충돌 처리 구조 정리
### 오늘 한 일
- SkillSystem, SkillBase, SkillEmission, ProjectileBase, ProjectileCollision2D의 역할을 흐름 중심으로 다시 확인했다.
- SkillSystem은 플레이어가 보유한 스킬들을 생성, 관리, 실행, 레벨업 요청하는 관리자 역할을 한다는 점을 정리했다.
- SkillBase는 모든 스킬이 공통으로 가져야 하는 데이터와 기능을 정의하는 부모 클래스이며, 스킬들을 공통 타입으로 관리하기 위한 구조라는 점을 확인했다.
- SkillEmission은 공격 스킬 실행 시 발사체를 생성하고, 생성된 발사체를 ProjectileBase 타입으로 다루는 흐름을 확인했다.
- ProjectileBase는 발사체의 공통 데이터와 실행 흐름을 제공하는 부모 클래스이며, 실제 이동 방식은 ProjectileStraight, ProjectileHoming 같은 자식 클래스에서 구현된다는 점을 정리했다.
- ProjectileCollision2D는 발사체의 이동이 아니라 충돌 감지 이후 대상 확인과 데미지 적용을 담당하는 클래스라는 점을 확인했다.
- ProjectileStraight와 ProjectileHoming의 차이를 비교하며, 직선 이동과 타겟 추적 이동의 차이를 정리했다.
- Mathf.Atan2가 방향 벡터의 x, y 값을 이용해 각도를 구하고, Unity 회전값에 적용하기 위해 Rad2Deg로 degree 단위 변환이 필요하다는 점을 확인했다.
### 배운 내용
- 스킬과 발사체는 각각 SkillBase, ProjectileBase를 통해 공통 타입으로 관리할 수 있다.
- 부모 클래스 타입으로 다루면 구체적인 자식 클래스에 직접 의존하지 않고, 공통 흐름으로 여러 스킬과 발사체를 처리할 수 있다.
- SkillSystem은 여러 스킬을 관리하는 시스템이고, SkillBase는 개별 스킬의 공통 구조를 정의하는 부모 클래스다.
- ProjectileBase는 발사체의 공통 구조를 제공하고, 실제 이동 방식은 자식 발사체 클래스에서 구현한다.
- ProjectileCollision2D는 발사체의 충돌 판정과 충돌 이후 데미지 처리를 분리하기 위한 클래스다.
- Mathf.Atan2는 방향 벡터를 회전 각도로 변환하기 위해 사용하며, 반환값이 라디안이기 때문에 Unity 회전에 사용하려면 degree 단위로 변환해야 한다.
### 막힌 부분
- 처음에는 ProjectileBase와 ProjectileCollision2D의 역할 범위가 명확하게 구분되지 않았다.
- ProjectileHoming, ProjectileCubicHoming처럼 수학 개념이 들어가는 스크립트는 방향 벡터, 각도 계산, 보간 개념 때문에 흐름 파악이 어려웠다.
### 해결한 방법
- 스크립트를 파일별로 따로 보지 않고, 스킬 실행 후 발사체가 생성되고 충돌하는 흐름으로 다시 정리했다.
- SkillSystem, SkillBase, SkillEmission, ProjectileBase, ProjectileCollision2D의 책임을 나누어 비교했다.
- 발사체 이동 방식과 충돌 처리를 분리해서 보면서, ProjectileBase와 ProjectileCollision2D의 역할 차이를 정리했다.
- Mathf.Atan2는 방향 벡터를 각도로 변환하기 위한 함수이고, Rad2Deg를 통해 Unity 회전값에 맞게 변환해야 한다는 점을 확인했다.
### 개인 메모
- 스킬 발사체 구조를 정리할 시간이 필요해서 오늘은 어제 학습한 내용을 바탕으로 흐름을 따라가며 정리하는 시간을 가졌다.
- 예제를 따라가면서 각 클래스들이 어떤 기준으로 분리를 했는지를 파악했고 각 클래스들이 갖는 의미를 다시 곱씹으며 예제의 내용을 이해했다.
- 수학적인 개념이 나오면 아직까지는 헷갈려서 이해하는데 시간이 걸리는데 추후에 학습을 진행할 필요가 있어보인다.
### 내일 할 일
- 보조공격에 대한 학습을 진행한다.
---
## 2026_06-20

### 오늘 목표
- 보조 공격 스킬의 구현 방식을 학습
- 주 공격 스킬과 보조 공격 스킬을 어떤 기준으로 구분할 수 있을지 생각한다.
### 오늘 한 일
- 보조 공격 스킬 예제 코드를 확인하며 발사체 생성과 이동 흐름을 따라갔다.
- SkillEmission에서 발사체를 생성하고, 발사체 타입에 따라 필요한 초기화 값을 전달하는 흐름을 확인했다.
- ProjectileQuadraticHoming 예제를 통해 베지어 곡선을 활용한 곡선형 발사체 이동 방식을 학습했다.
- RotateEffect 예제를 통해 이펙트 오브젝트를 회전시키는 간단한 보조 연출 구조를 확인했다.
### 배운 내용
- 보조 공격은 주 공격처럼 발사체마다 완전히 다른 공격 방식을 만드는 구조라기보다, 공통된 공격 방식과 이동 방식을 재사용하면서 속성(Element)의 차이를 주는 방식에 가까웠다.
- 기존 스킬 시스템을 크게 수정하지 않고, 발사체 클래스나 초기화 데이터만 확장해 새로운 공격 방식을 추가할 수 있다는 점을 확인했다.
- 곡선형 발사체 이동에는 베지어 곡선과 같은 수학적 개념이 포함되어 있어 아직 완전히 익숙하지는 않지만, 코드 흐름상으로는 Setup에서 이동에 필요한 값을 준비하고 Process에서 시간에 따라 위치를 갱신하는 구조로 이해했다.
- 보조 공격 발사체는 전체 발사체 개수와 현재 발사 순번을 기준으로 제어점의 각도를 계산하고, 이 제어점을 이용해 곡선 경로를 만든다.
### 막힌 부분
- 베지어 곡선과 제어점 계산 방식은 수학적인 개념이 포함되어 있어 한 번에 완전히 이해하기 어려웠다.
### 해결한 방법
- 수학 공식 자체를 완전히 이해하려 하기보다, 먼저 코드의 실행 흐름을 중심으로 학습했다.
- 발사체가 생성될 때 어떤 값이 전달되고, 이동 중 어떤 값이 갱신되는지에 집중해 구조를 파악했다.
- 수학적 개념은 이후 필요한 부분부터 따로 정리하며 익숙해지기로 했다.
### 개인 메모
- 주 공격 스킬은 발사체마다 공격 방식과 특성이 드러나는 구조로 만들 수 있고, 보조 공격 스킬은 공통 구조를 재사용하면서 속성이나 보조 효과로 차이를 줄 수 있을 것 같다.
- 보조 공격은 공격 방식과 이동 방식을 모두 다르게 만들기보다, 공통 Property를 유지하고 Element 차이로 구분하면 효율적으로 확장할 수 있을 것 같다.
### 내일 할 일
- 플레이어 주변을 회전하며 적을 공격하는 공격 방식에 대해 학습한다.
- 회전형 공격이 기존 발사체 구조와 어떻게 다른지 확인한다.
- 플레이어 중심 회전 공격을 스킬 구조 안에서 어떻게 처리하는지 흐름을 따라가며 정리한다.
---
## 2026-06-21

### 오늘 목표 
- 책 예제를 따라 회전형 근접 공격 구조 학습
### 오늘 한 일
- 책 예제를 통해 플레이어 주변을 회전하는 근접 공격 스킬 구조를 확인했다.
- 회전형 근접 공격 스킬도 기존 스킬들과 동일하게 SkillSystem에서 관리되는 흐름을 확인했다.
- Sustained 타입 스킬이 SkillSystem에서 생성되고, SkillSustained에서 근접 무기 오브젝트를 생성하고 배치하는 흐름을 살펴봤다.
- ProjectileCollision2D를 통해 회전형 근접 무기의 충돌 판정과 데미지 처리가 이루어지는 구조를 확인했다.
### 배운 내용
- 회전형 근접 공격은 거리와 각도를 기준으로 Cos, Sin을 사용해 원 둘레의 위치를 계산하는 방식으로 배치할 수 있다.
- 근접 무기의 개수에 따라 360도를 나누고, 각 무기마다 서로 다른 각도를 부여하면 일정한 간격으로 배치할 수 있다.
- 새 근접 무기가 추가될 때 전체 무기 개수를 기준으로 각도를 다시 계산하면 무기 사이의 간격을 자연스럽게 유지할 수 있다.
- 실제 회전은 각 무기의 위치를 매 프레임 직접 다시 계산하는 방식이 아니라, 부모 오브젝트를 회전시켜 자식 오브젝트들이 함께 회전하도록 처리할 수 있다.
- SkillSystem이 스킬 생성과 레벨업을 관리하고, SkillSustained가 회전형 근접 공격의 생성과 배치를 담당하며, ProjectileCollision2D가 충돌과 데미지 처리를 담당하는 식으로 역할이 분리되어 있다.
- 예제 코드의 흐름을 따라가며 SkillSystem, SkillSustained, Utils, RotateEffect, ProjectileCollision2D의 역할을 나누어 확인했다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- SkillSystem이 중간 관리 역할을 하기 때문에 새로운 스킬 타입이 추가되어도 전체 구조를 크게 바꾸지 않고 타입별 스킬 클래스를 추가하는 방식으로 확장할 수 있다는 점을 확인했다.
- 회전형 근접 공격은 알고리즘 자체가 복잡하다기보다, 원형 배치 계산과 부모 오브젝트 회전을 분리해서 이해하는 것이 중요해 보인다.
- 단순한 예제라도 어떤 클래스가 어떤 책임을 가지는지 나누어 보면 이후 기능을 설계할 때 참고할 수 있을 것 같다.
### 내일 할 일
- 책 예제를 통해 전 범위 타격 스킬이 어떤 방식으로 구현되는지 학습한다.
- 유도 공격 패턴을 확장할 때 필요한 구조와 처리 흐름을 확인한다.
---
## 2026-06-22

### 오늘 목표 
- 새로운 주 공격 스킬을 추가하는 방법 학습
### 오늘 한 일
- 예제 스크립트를 통해 Global 계열 주 공격 스킬의 추가 흐름을 확인했다.
- SkillSystem에서 SkillType.Global 스킬을 생성하고 관리하는 구조를 확인했다.
- SkillGlobal이 스킬 레벨업과 발사체 생성을 담당하고, ProjectileGlobal 계열 클래스가 실제 공격 처리를 담당하는 흐름을 확인했다.
- 여러 전체 공격 스킬들이 ProjectileGlobal을 상속받아 공통 기능을 재사용하는 구조를 학습했다
### 배운 내용
- 비슷한 성격의 스킬들은 공통 베이스 클래스를 만들어 중복되는 속성과 처리를 모아둘 수 있다.
- SkillGlobal은 주 공격 스킬의 레벨업 처리와 발사체 생성을 담당한다.
- ProjectileGlobal은 데미지 처리, 지속 시간, 공격 주기, 피격 이펙트 같은 공통 처리를 담당한다.
- 각 스킬 클래스는 공통 기능을 다시 작성하지 않고, 필요한 공격 방식만 오버라이드해서 구현한다.
- 같은 전체 공격 계열이라도 공격 대상 탐색, 이동 방식, 데미지 적용 타이밍은 스킬마다 다르게 설계될 수 있다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 오늘 예제에서도 부모 클래스를 통해 공통 처리를 묶고, 자식 클래스에서 차이점만 구현하는 구조가 반복되었다.
- 아직 직접 설계하는 것은 미숙하지만, 공통 기능을 베이스 클래스로 분리해 재사용성과 확장성을 높이는 방식에 조금씩 익숙해지고 있다.
- 내 프로젝트에서 여러 스킬을 구현할 때도 먼저 공통 처리와 개별 처리를 구분한 뒤, 필요한 경우 베이스 클래스를 만들어 상속받는 구조를 참고하면 좋을 것 같다.
### 내일 할 일
- 주 공격 스킬 구조를 프로젝트에 적용
---
## 2026-06-23

### 오늘 목표
- 예제의 주 공격 스킬을 프로젝트에 적용하면서 발사체 생성 흐름을 복습
### 오늘 한 일
- 예제에서 학습한 주 공격 스킬 구조를 프로젝트에 다시 적용했다.
- SkillEmission에서 발사체가 일정 간격으로 생성되고, 설정된 발사체 개수만큼 발사한 뒤 쿨타임에 들어가는 흐름을 확인했다.
- CubicHoming 발사체에서 GetNewPoint로 제어점을 만들고, CubicCurve를 통해 곡선 경로를 따라 이동하는 흐름을 확인했다.
- 주 공격 스킬에 치명타를 적용할 수 있을지 검토했지만, 현재 단계에서는 적용하지 않기로 했다.
### 배운 내용
- GetNewPoint는 시작점을 기준으로 특정 각도와 거리만큼 떨어진 새 점을 구하는 함수다.
- Lerp는 두 점 사이의 위치를 구하고, QuadraticCurve와 CubicCurve는 Lerp를 반복해서 곡선 위의 위치를 계산한다.
- CubicHoming 발사체는 시작점, 제어점, 목표 지점을 이용해 직선이 아닌 곡선 이동을 만든다.
- 치명타를 발사체 스킬에 적용하려면 데미지뿐만 아니라 치명타 여부까지 전달해야 하므로, 단순 수정으로 끝나지 않을 수 있다.
### 막힌 부분
- GetNewPoint가 CubicCurve의 제어점 계산에 어떻게 사용되는지 바로 이해하기 어려웠다.
- 주 공격 스킬에 치명타를 추가하려면 Setup 메서드의 전달값을 확장해야 할 것 같았고, 수정 범위가 커질 수 있다고 느꼈다.
- 이미 높은 데미지를 가진 스킬에 치명타까지 적용하면 밸런스가 크게 무너질 수 있다고 판단했다.
### 해결한 방법
- GetNewPoint는 시작점을 중심으로 원 위의 한 점을 구하는 함수로 정리했다.
- Lerp, QuadraticCurve, CubicCurve는 선형 보간을 반복해 곡선 위의 위치를 구하는 흐름으로 이해했다.
- 치명타 적용은 현재 단계에서 보류하고, 먼저 예제의 주 공격 스킬 흐름을 이해하는 데 집중하기로 했다.
### 개인 메모
- 치명타 같은 확장 요소는 지금 바로 추가하기보다, 나중에 공격 데이터 구조를 따로 정리할 때 다시 검토하는 편이 적절해 보인다.
### 내일 할 일
- 보조 공격 스킬을 프로젝트에 적용해본다.
- 보조 공격 스킬이 생성되고 동작하는 흐름을 확인한다.
---
## 2026-06-24

### 오늘 목표
- 보조 공격 스킬 예제 구조를 프로젝트에 적용하고, 스킬 데이터 호출 흐름과 발사체 이동 방식을 확인한다.
### 오늘 한 일
- SkillEmission을 통해 SkillTemplate에 저장된 발사체 프리팹을 불러와 스킬을 실행하는 흐름을 확인했다.
- 스킬 사용 시 ProjectileCount 값을 기준으로 여러 발사체를 순차적으로 생성하도록 구현했다.
- 보조 공격 발사체인 ProjectileQuadraticHoming을 추가했다.
- ProjectileQuadraticHoming에서 시작 위치, 목표 위치, 중간 제어점을 사용해 곡선 형태로 이동하도록 구현했다.
- RotateEffect를 추가해 발사체 또는 이펙트 오브젝트가 일정 속도로 회전하도록 구현했다.
### 배운 내용
- 보조 공격 스킬은 기본 공격과 완전히 다른 구조가 아니라, 기존 스킬 발사 구조를 재사용하면서 발사체 이동 방식만 다르게 처리할 수 있다.
- SkillTemplate에 저장된 발사체 프리팹을 SkillEmission에서 생성하면, 스킬 데이터와 실제 스킬 실행 로직을 분리해서 관리할 수 있다.
- 발사체 개수와 현재 발사 순번을 함께 사용하면 같은 스킬 안에서도 발사체마다 다른 각도와 경로를 만들 수 있다.
- ProjectileQuadraticHoming은 목표 위치로 직선 이동하는 대신, 중간 제어점을 거쳐 C자 형태의 곡선 경로로 이동한다.
- 발사체의 이동 방식이 달라져도 공통 발사체 구조를 유지하면 스킬 시스템 전체를 크게 수정하지 않고 새로운 공격 방식을 추가할 수 있다.
### 막힌 부분
- 보조 공격 스킬이 기본 공격과 큰 차이가 없어 DevLog에 기록할 내용을 정리하는 데 고민이 있었다.
### 해결한 방법
- 공격 판정 자체보다 SkillTemplate 데이터를 SkillEmission에서 호출하고, 발사체 이동 방식이 분리되는 흐름을 중심으로 정리했다.
- 보조 공격의 차이점을 “새로운 공격 규칙”이 아니라 “기존 발사 구조를 재사용한 곡선 이동 발사체”로 정리했다.
### 개인 메모
- 중요한 보조 공격 스킬이지만 기본 탄환에서 C자 곡선 형태로 이동하고 회전하는 것 외에는 큰 차이가 없었다.
- 공격 방식 자체는 기본 공격에 속성을 부여한 형태에 가깝고, 핵심 차이는 SkillTemplate에 저장된 데이터를 SkillEmission에서 불러와 스킬을 실행하는 흐름을 확인한 점이다.
### 내일 할 일
- 플레이어 주변을 회전하며 적을 공격하는 근접 공격 스킬 예제 구조를 프로젝트에 적용한다.
- 회전 공격 오브젝트가 플레이어를 기준으로 따라다니며 공격 판정을 처리하는 흐름을 확인한다.
- 기존 SkillTemplate, SkillEmission, Projectile 구조와 어떤 차이가 있는지 정리한다.
---
## 2026-06-25

### 오늘 목표
- 플레이어 주위를 회전하는 곡괭이 스킬 예제를 프로젝트에 적용한다.
### 오늘 한 일
- `DestroyType`에 `Indestructible`을 추가해 충돌 후에도 파괴되지 않는 공격 오브젝트를 구분할 수 있도록 했다.
- `SkillType`에 `Sustained`를 추가해 지속형 스킬을 구분할 수 있도록 했다.
- 지속형 스킬은 일반 발사형 스킬처럼 쿨타임마다 실행되는 구조가 아니므로, `SkillBase.IsSkillAvailable()`에서 쿨타임 계산 대상에서 제외했다.
- `Utils.GetPositionFromAngle()`을 추가해 반지름과 각도를 기준으로 원 둘레 위치를 구할 수 있도록 했다.
- `SkillBase`를 상속받는 `SkillSustained` 클래스를 추가했다.
- `SkillSystem`에서 `SkillType.Sustained` 타입의 스킬 데이터를 불러올 때 `SkillSustained` 인스턴스를 생성하도록 연결했다.
- 곡괭이 프리팹을 생성한 뒤 `Pickaxs` 부모 오브젝트의 자식으로 배치하도록 구성했다.
- 곡괭이 스킬 레벨업 시 생성된 곡괭이들의 위치를 다시 계산해 플레이어 주변에 배치되도록 했다.
- 곡괭이가 적과 충돌했을 때 데미지를 주되, `Indestructible` 타입일 경우 파괴되지 않도록 처리했다.
- 곡괭이 스킬 레벨업 시 공격력 관련 스탯을 갱신하고, 이미 생성된 곡괭이들의 데미지 값도 다시 설정하도록 했다.
### 배운 내용
- 지속형 스킬은 `OnSkill()`에서 반복적으로 발사체를 생성하는 방식이 아니라, 레벨업 시 생성된 오브젝트가 유지되는 방식으로 동작한다.
- 기존 발사형 스킬은 쿨타임을 기준으로 발동되지만, 곡괭이 스킬은 유지형 오브젝트이기 때문에 쿨타임 계산 흐름에서 제외하는 처리가 필요했다.
- `ProjectileCollision2D`의 `DestroyType`을 확장해 발사체의 파괴 여부를 스킬 특성에 따라 다르게 처리할 수 있었다.
- `SkillSustained`는 지속형 공격 오브젝트의 생성, 배치, 데미지 갱신을 담당하는 클래스로 분리했다.
### 막힌 부분
- `SkillSustained.OnLevelUp()`에서 곡괭이 위치를 설정할 때 `parent.position + position`을 사용하는 이유가 헷갈렸다.
### 해결한 방법
- `Utils.GetPositionFromAngle()`이 반환하는 `position`은 원점 기준의 둘레 위치이고, 여기에 `parent.position`을 더해 부모 오브젝트 위치를 중심으로 한 월드 좌표로 변환하는 구조로 이해했다.
- 결과적으로 곡괭이 오브젝트를 `Pickaxs` 부모 오브젝트 주변에 일정 거리만큼 떨어뜨려 배치하기 위한 처리로 정리했다.
### 개인 메모
- 이번 작업은 새로운 개념을 학습하는 것보다, 이전에 학습한 지속형 곡괭이 스킬 구조를 프로젝트에 적용하는 과정에 가까웠다.
- 발사 후 사라지는 공격과 달리, 플레이어 주변에 유지되는 공격 오브젝트는 스킬 실행 시점보다 생성 이후의 유지 방식과 충돌 처리 방식이 더 중요해 보인다.
- 지속형 스킬을 별도 `SkillSustained` 클래스로 분리하면서, 스킬 타입에 따라 생성과 실행 흐름이 달라질 수 있다는 점을 다시 확인했다.
### 내일 할 일
- 전역 스킬 예제를 프로젝트에 적용한다.
- 전역 스킬이 기존 스킬 시스템에서 어떤 방식으로 생성되고 실행되는지 확인한다.
- 전역 스킬의 공격 범위, 데미지 처리, 실행 흐름을 확인한다.
---
## 2026-06-26

### 오늘 목표
- 전 범위 타격 스킬과 유도 공격 패턴 스킬 예제를 프로젝트에 적용하고, 각 스킬의 특성과 구조를 확인한다.
- 스킬 생성, 실행, 발사체 처리 흐름을 따라가며 추후 직접 구조를 설계하기 위한 기반을 다진다.
### 오늘 한 일
- 전역 공격 스킬을 처리하기 위한 SkillGlobal 스크립트를 추가했다.
- 전역 공격 발사체의 공통 부모 역할을 하는 ProjectileGlobal 스크립트를 추가했다.
- ProjectileGlobal을 상속받는 전역 공격 스킬 발사체들을 추가했다.
    - ProjectileBlizzard
    - ProjectileLightningStrike
    - ProjectileFireDragon
    - ProjectileTornado
    - ProjectileVoid
- SkillTemplate, SkillSystem, SkillBase, ProjectileBase, Stat 스크립트를 수정했다.
- SkillSystem에서 SkillType.Global 타입의 스킬을 SkillGlobal로 생성하도록 흐름을 추가했다.
- SkillGlobal에서 쿨타임이 준비되면 스킬 프리팹을 생성하고, ProjectileGlobal.Setup()을 통해 데미지 정보를 전달하도록 구현했다.
### 배운 내용
- 전역 공격 스킬은 SkillGlobal이 공통 실행 흐름을 담당하고, 실제 공격 방식은 ProjectileGlobal을 상속받은 개별 발사체 클래스에서 처리하는 구조로 분리할 수 있다.
- ProjectileGlobal은 ProjectileBase를 상속받아 발사체의 공통 기능을 유지하면서, 전역 공격 스킬에 필요한 지속 시간, 데미지, 피격 이펙트, 데미지 텍스트 생성 흐름을 공통으로 관리한다.
- ProjectileBlizzard는 플레이어 주변에 유지되며 일정 주기마다 범위 내 적에게 피해를 주는 지속형 전역 공격 구조로 구현되었다.
- ProjectileLightningStrike는 필드의 적 위치에 즉시 공격 이펙트를 생성하고 피해를 주는 즉발형 전역 공격 구조로 구현되었다.
- ProjectileFireDragon은 적 목록을 기준으로 공격 처리 순서를 관리하며, 공격 오브젝트가 이동하면서 적에게 피해를 주는 방식으로 구현되었다.
- ProjectileTornado는 목표를 추적하다가 대상이 사라지면 새로운 대상을 탐색하는 유도형 공격 구조로 구현되었다.
- ProjectileVoid는 목표 방향으로 이동하면서 일정 주기마다 원형으로 발사체를 생성하는 방사형 공격 구조로 구현되었다.
- 같은 전역 공격 스킬이라도 생성 위치, 대상 탐색 방식, 피해 적용 시점, 지속 시간 처리 방식에 따라 서로 다른 공격 패턴을 만들 수 있다는 점을 확인했다.
### 막힌 부분
- ProjectileFireDragon의 Process()에서 entities[0]이 null일 때 entities.RemoveAt(0)을 실행하는 이유가 처음에는 명확하게 이해되지 않았다.
### 해결한 방법
- `entities`는 시전 시점의 적 목록을 복사한 것이므로, 0번 대상이 다른 공격으로 사망해 `null`이 되면 해당 요소를 제거해 다음 대상을 처리하도록 한다.
### 개인 메모
- ProjectileGlobal은 ProjectileBase를 상속받고, 전역 공격과 유도 공격 스킬은 다시 ProjectileGlobal을 상속받는 구조로 확장되었다.
- 단순히 예제를 따라 적용하는 것보다, 어떤 로직을 부모 클래스에 두고 어떤 로직을 자식 클래스에서 처리하는지 구분하는 것이 중요하다고 느꼈다.
### 내일 할 일
- 각 스킬들의 레벨 합계에 따라서 자동으로 보너스를 얻도록 하는 방법을 학습한다.
- 습득 가능한 모든 스킬 목록을 UI로 구현하는 방법을 학습한다.
- 학습 내용을 바탕으로 이후 작업에서 헷갈리지 않도록 흐름과 내용을 파악한다.
---
## 2026-06-27

### 오늘 목표 
- 조건 기반 속성 시너지 시스템과 스킬 자동 성장 로직 예제의 흐름을 확인한다.
### 오늘 한 일
- 조건 기반 속성 시너지 시스템 예제 구조를 확인했다.
- 일반 스킬과 속성 보너스 스킬을 분리해서 등록하는 흐름을 확인했다.
- 일반 스킬 레벨업 시 해당 스킬의 속성 카운터를 증가시키고, 조건을 만족하면 속성 보너스 스킬이 자동으로 성장하는 구조를 확인했다.
- 특정 속성 보너스가 데미지 계산에 반영되는 흐름을 확인했다.
### 배운 내용
- 일반 스킬은 Resources/Skills에서 로드되어 선택 및 레벨업 대상이 된다.
- 속성 보너스 스킬은 Resources/ElementalSkills에서 로드되어 속성별 보너스 스킬로 따로 등록된다.
- elementalCounts는 속성별 스킬 레벨업 누적 횟수를 관리하고, elementalSkills는 조건을 만족했을 때 성장할 속성 보너스 스킬을 관리한다.
- 일반 스킬이 레벨업할 때마다 해당 속성의 카운터가 증가하고, 일정 조건을 만족하면 같은 속성의 보너스 스킬이 자동으로 레벨업된다.
- 데미지 계산 시 스킬의 속성에 맞는 속성 보너스 값을 함께 반영할 수 있다.
### 막힌 부분
- 속성 보너스 스킬이 일반 스킬처럼 등록되는지, 별도의 자동 성장용 스킬로 관리되는지 헷갈렸다.
- 스킬 레벨업 시 증가하는 elementalCounts가 실제 스킬 레벨 합을 다시 계산한 값인지, 레벨업 누적 횟수를 저장하는 값인지 헷갈렸다.
### 해결한 방법
- SkillSystem의 스킬 등록 흐름을 일반 스킬 등록과 속성 보너스 스킬 등록으로 나누어 확인했다.
- LevelUp() 흐름을 따라가며 일반 스킬 레벨업, 속성 카운터 증가, 조건 충족 시 속성 보너스 스킬 레벨업 순서로 정리했다.
### 개인 메모
- StatType의 각 속성 보너스 스탯 상수와 SkillElement의 상수 값을 일치시켜, 형변환을 통해 보너스 데미지를 계산하는 방식이 인상적이었다.
- 추후에 해당 구조를 다시 구현해보고, 프로젝트에 적용할 수 있는 방식인지 확인해보고 싶다.
### 내일 할 일
- 습득 가능한 모든 스킬을 UI에 표시하는 방법에 대해서 학습한다.
---
## 2026-06-28

### 오늘 목표
- 습득 가능한 모든 스킬 목록을 UI에 표시하는 예제 구조를 학습
### 오늘 한 일
- SkillSystem에서 저장된 SkillTemplate 데이터를 불러온 뒤, 모든 스킬 정보를 UI에 표시하는 흐름을 확인했다.
- 스킬 목록 UI가 초기화될 때 UISkillList의 Setup을 통해 스킬 아이콘들이 생성되는 구조를 확인했다.
- 스킬 습득 또는 레벨업이 발생했을 때, 해당 스킬의 UI 아이콘과 레벨 표시가 갱신되는 흐름을 확인했다.
- 일반 스킬과 속성 보너스 스킬이 각각 다른 UI 영역에 배치되는 구조를 확인했다.
### 배운 내용
- 스킬 목록 UI는 개별 스킬 데이터를 직접 생성하지 않고, SkillSystem에서 관리하는 스킬 정보를 전달받아 표시한다.
- UISkillList는 전체 스킬 목록을 관리하고, UISkillIcon은 개별 스킬의 아이콘과 레벨 표시를 담당한다.
- 스킬의 현재 상태는 SkillBase를 통해 확인하고, UI는 해당 정보를 기반으로 표시 상태를 갱신한다.
- 에디터에서 UI를 구성할 때 Horizontal Layout Group과 Grid Layout Group을 활용하면 여러 UI 요소를 일정한 규칙에 따라 배치할 수 있다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 에디터에서 UI 작업을 할 때 구성하는 컴포넌트에 좀 더 익숙해질 필요가 있어 보인다.
- 오늘 학습에서 UI를 만들 때 주로 사용한 컴포넌트는 Horizontal Layout Group과 Grid Layout Group이었지만, 아직 익숙하지 않아서 추후 반복해서 다뤄볼 필요가 있다.
### 내일 할 일
- 무작위로 스킬을 배우고 레벨업하는 예제를 통해 스킬 습득과 성장 흐름을 학습한다.
---
## 2026-06-29

### 오늘 목표
- 무작위 스킬 습득 및 스킬 레벨업 예제 흐름을 학습한다.
- 레벨업 시 스킬 후보가 선택되고, UI를 통해 선택한 스킬이 레벨업되는 흐름을 확인한다.
### 오늘 한 일
- 무작위 스킬 선택 UI 예제 흐름을 학습했다.
- SkillSystem에서 1번 키 입력 시 스킬 선택 UI를 열도록 구성된 흐름을 확인했다.
- 최대 레벨에 도달하지 않은 스킬 중 무작위 후보를 추려 선택 UI에 표시하는 흐름을 확인했다.
- UISelectSkill을 통해 스킬 선택 패널을 활성화하고, 각 스킬 후보 정보를 아이콘 UI에 전달하는 구조를 확인했다.
- UISelectSkillIcon을 통해 스킬 아이콘, 이름, 설명을 표시하고, 클릭 시 선택된 스킬을 SkillSystem으로 전달하는 흐름을 확인했다.
- 선택된 스킬은 SkillSystem.EndSelectSkill()을 통해 LevelUp() 처리 후 UI를 닫고 게임을 재개하는 흐름을 확인했다.
### 배운 내용
- 스킬 선택 후보는 전체 스킬 중 최대 레벨에 도달하지 않은 스킬을 기준으로 추려낼 수 있다.
- 아직 습득하지 않은 스킬도 CurrentLevel이 0인 상태에서 LevelUp()을 호출하면 신규 습득처럼 처리할 수 있다.
- 스킬 선택 UI는 선택된 스킬 정보를 전달하는 역할을 맡고, 실제 레벨업 처리는 SkillSystem에서 담당하도록 분리할 수 있다.
- IPointerClickHandler를 사용하면 UI 오브젝트 클릭 시 OnPointerClick()을 통해 선택 이벤트를 처리할 수 있다.
- UI 클릭 처리를 개별 아이콘 클래스에서 담당하게 하면, 스킬 선택 UI처럼 여러 선택지를 가진 구조에서 활용하기 좋다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- IPointerClickHandler를 이용해 SelectSkillIcon 클릭 시 콜백을 처리하는 방식이 활용하기 좋아 보였다.
- UI 오브젝트 단위로 클릭 이벤트를 처리할 수 있어 추후 스킬 선택 UI나 인벤토리 UI 같은 구조에서도 다시 활용할 수 있을 것 같다.
### 내일 할 일
- 레벨업 시스템 흐름을 학습한다.
- 메모리 풀링을 활용한 최적화 방법을 학습한다.
---
## 2026-06-30

### 오늘 목표
- 레벨업 시스템 예제를 학습한다.
### 오늘 한 일
- 레벨업 시스템 예제 학습을 진행했다.
- 적 처치 시 EnemyBase.OnDie()에서 플레이어의 AccumulationExp에 경험치가 누적되는 흐름을 확인했다.
- PlayerBase.UpdateEXP()에서 누적된 경험치를 일정량씩 CurrentExp로 반영하는 흐름을 확인했다.
- CurrentExp.OnValueChanged 이벤트를 통해 레벨업 판정과 경험치 UI 갱신이 연결되는 흐름을 확인했다.
- PlayerBase.IsLevelUP()에서 현재 경험치가 필요 경험치에 도달하면 레벨을 증가시키고, LevelData를 통해 다음 레벨의 필요 경험치를 다시 설정하는 흐름을 확인했다.
- 레벨업 이후 SkillSystem.StartSelectSkill()을 호출해 스킬 선택 UI로 이어지는 구조를 확인했다.
- UIPlayerData에서 경험치 변경 이벤트를 구독해 레벨 텍스트와 경험치 게이지를 갱신하는 흐름을 확인했다.
### 배운 내용
- 적 처치로 얻은 경험치를 바로 현재 경험치에 더하지 않고, AccumulationExp에 먼저 저장한 뒤 일정량씩 흡수하는 방식을 확인했다.
- 경험치 흡수 흐름을 분리하면 경험치 UI를 단계적으로 갱신할 수 있고, 스킬 선택 중에는 경험치 처리를 일시적으로 멈출 수 있다.
- OnValueChanged 이벤트를 활용하면 경험치 값 변경을 기준으로 레벨업 판정과 UI 갱신을 연결할 수 있다.
- LevelData를 ScriptableObject로 분리해 레벨별 필요 경험치를 데이터로 관리하는 방식을 확인했다.
- as를 사용하면 특정 타입으로 변환 가능한 경우 해당 타입으로 변환하고, 변환할 수 없는 경우 null을 반환한다.
- Unity의 Mask 컴포넌트는 자식 UI 오브젝트의 실제 크기나 형태를 바꾸는 것이 아니라, 부모 UI의 마스크 영역 안에서만 보이도록 표시 범위를 제한하는 컴포넌트임을 확인했다.
### 막힌 부분
- 경험치를 바로 CurrentExp.DefaultValue에 더하지 않고 AccumulationExp에 임시로 저장한 뒤 프레임마다 정해진 값만큼 반영하는 이유가 헷갈렸다.
- EnemyBase.OnDie()의 Target as PlayerBase 구문이 오래전에 학습한 개념이라 다시 확인이 필요했다.
- Unity에서 제공하는 Mask 컴포넌트가 어떤 역할을 하는지 확인이 필요했다.
### 해결한 방법
- AccumulationExp를 사용하면 경험치 획득과 실제 경험치 반영 흐름을 분리할 수 있고, 경험치 UI를 단계적으로 갱신할 수 있음을 확인했다.
- 스킬 선택 중에는 UpdateEXP()가 경험치 흡수를 중단하므로, 레벨업 처리와 스킬 선택 흐름을 제어하기 위한 구조로 볼 수 있음을 확인했다.
- as는 타입 변환이 가능하면 변환된 값을 반환하고, 변환할 수 없으면 null을 반환한다는 점을 다시 확인했다.
- Unity의 Mask 컴포넌트는 자식 UI가 부모 UI의 마스크 영역 안에서만 보이도록 표시 범위를 제한하는 역할임을 확인했다.
### 개인 메모
- as를 통한 타입 검사와 변환 방식은 추후 프로젝트 적용을 검토해볼 만하다.
- 다만 as를 사용할 경우 변환 실패 시 null이 반환되므로, 실제 적용할 때는 null 처리까지 함께 고려해야 한다.
- 레벨업 시스템의 전체 구조는 추후 프로젝트에 직접 적용하면서 다시 확인할 필요가 있다.
### 내일 할 일
- 메모리 풀링 기법을 활용해 자원을 효율적으로 관리하는 방법을 학습한다.
---
## 2026-07-01

### 오늘 목표
- 메모리 풀을 활용해 리소스를 효율적으로 관리하는 예제 흐름을 학습한다.
- 보석 이펙트 생성, 이동, 수집, 반환 흐름을 확인한다.
### 오늘 한 일
- MemoryPool, GemCollector, GemCollectEffect, FadeEffect의 역할과 호출 흐름을 확인했다.
- 적 사망 시 GemCollector가 메모리 풀에서 보석 이펙트를 활성화하고, 수집 완료 후 다시 비활성화하는 구조를 확인했다.
- 보석 이펙트가 UI GemIcon을 향해 이동하고, 도착 시 이벤트를 통해 보석 획득 처리를 호출하는 흐름을 확인했다.
### 배운 내용
- 메모리 풀은 반복적인 Instantiate / Destroy 대신 오브젝트를 미리 생성하고 재사용해 리소스 사용을 줄이는 구조다.
- MemoryPool은 활성/비활성 상태를 기준으로 오브젝트를 꺼내고 반환하며, 부족할 경우 일정 개수만큼 추가 생성한다.
- RectTransformUtility를 사용하면 UI 위치와 월드 좌표 사이의 변환을 처리해 월드 오브젝트가 UI 위치를 향해 이동하도록 만들 수 있다.
### 막힌 부분
- RectTransformUtility.WorldToScreenPoint()와 ScreenPointToWorldPointInRectangle()을 이용한 좌표 변환 흐름이 아직 익숙하지 않다.
### 해결한 방법
- 예제 실행을 통해 적 사망, 보석 이펙트 생성, UI 아이콘으로 이동, 보석 획득 처리까지의 정상 동작 흐름을 확인했다.
- 좌표 변환 개념은 아직 완전히 이해되지 않아, 개인 메모에 따로 정리해두고 여러 번 다시 확인하기로 했다.
### 개인 메모
- 메모리 풀의 기본 흐름은 이해했지만, UI 좌표와 월드 좌표를 변환하는 과정은 아직 익숙하지 않다.
- RectTransformUtility 관련 개념은 자세히 정리해두고 여러 번 보면서 흐름을 익힐 필요가 있다.
### 내일 할 일
- 속성 시너지 예제를 실제 프로젝트에 적용해보고 흐름을 확인한다.
---
## 2026-07-02

### 오늘 목표
- 스킬 레벨에 따른 속성 보너스 자동 획득 예제 구조를 프로젝트에 적용한다.
- 스킬 레벨업 횟수와 속성 보너스 스탯이 연결되는 흐름을 확인한다.
- 나중에 다시 확인했을 때 자동 보너스가 어떤 흐름으로 적용되는지 파악할 수 있도록 정리한다.
### 오늘 한 일
- 스킬 레벨에 따른 속성 보너스 자동 획득 구조를 프로젝트에 적용했다.
- SkillSystem에서 스킬 레벨업 횟수를 속성별로 카운트하는 구조를 추가했다.
- 같은 속성의 스킬 레벨업 횟수가 3의 배수가 될 때마다 해당 속성 보너스 스킬이 자동으로 레벨업되도록 처리했다.
- 속성 보너스 스킬을 별도로 불러와 elementalSkills에 저장하고, 속성별 레벨업 횟수는 elementalCounts로 관리하도록 했다.
- SkillBase에 CalculateDamage()를 추가해 기본 데미지에 속성 보너스 스탯을 반영한 최종 데미지를 계산하도록 했다.
- SkillEmission, SkillSustained에서 발사체에 데미지를 전달할 때 기존 Damage.Value 대신 CalculateDamage()를 사용하도록 수정했다.
- StatType에 속성 보너스 스탯 값을 추가하고, SkillElement와 값을 맞춰 형변환으로 해당 속성 보너스 스탯을 찾는 구조를 확인했다.
- SkillSustained에 prevDamage를 추가하고, OnSkill()에서 현재 데미지와 이전 데미지를 비교해 값이 바뀐 경우에만 곡괭이 데미지를 갱신하도록 수정했다.
### 배운 내용
- SkillSystem이 스킬의 레벨업 횟수를 속성별로 카운트하고, 조건을 만족하면 해당 속성의 보너스 스탯을 레벨업시키는 흐름을 이해했다.
- 속성 보너스는 직접 선택하는 일반 스킬이 아니라, 같은 속성 스킬의 레벨업 횟수가 누적되었을 때 자동으로 성장하는 보조 구조로 처리된다.
- 공격 스킬은 발사체를 생성할 때 CalculateDamage()를 호출하고, 이 함수에서 기본 데미지와 속성 보너스 스탯을 함께 계산한다.
- SkillElement와 StatType의 값을 일치시키면 형변환을 통해 해당 속성 보너스 스탯을 쉽게 찾을 수 있다.
- 다만 enum 값이 어긋나면 잘못된 스탯을 찾을 수 있으므로, 이 방식은 사용할 때 값 관리에 주의가 필요하다.
- SkillSustained처럼 이미 생성된 오브젝트가 데미지를 저장하는 구조는, 스탯 변경 시점과 데미지 갱신 시점을 따로 고려해야 한다.
- 현재 구조에서는 SkillSystem이 보유 중인 스킬의 OnSkill()을 계속 호출하므로, SkillSustained.OnSkill()에서 데미지 변경 여부를 확인하고 필요한 경우에만 갱신하는 방식으로 처리할 수 있다.
### 막힌 부분
- SkillSustained는 데미지 갱신 타이밍이 자기 자신의 레벨업에만 존재했기 때문에, 속성 보너스 스탯이 증가해도 이미 생성된 곡괭이들의 데미지가 바로 갱신되지 않는 문제가 있었다.
- 일반 스킬 레벨업 과정에서 지속형 스킬의 데미지가 먼저 갱신되고, 이후에 속성 보너스 스탯이 레벨업되면 보너스 값이 반영되기 전의 데미지가 적용될 수 있는지 확인이 필요했다.
### 해결한 방법
- SkillSystem이 SkillSustained를 직접 갱신하지 않고, SkillSustained 내부에서 데미지 변경 여부를 판단하도록 수정했다.
- SkillSustained에 prevDamage를 추가하고, OnSkill()에서 CalculateDamage() 결과와 비교해 데미지가 바뀐 경우에만 기존 곡괭이들의 데미지를 다시 세팅하도록 했다.
- 이를 통해 속성 보너스 증가나 스킬 레벨업으로 최종 데미지가 변경되었을 때, 지속형 스킬이 스스로 데미지를 갱신할 수 있도록 처리했다.
### 개인 메모
- SkillElement와 StatType의 값을 일치시켜 형변환으로 스탯을 찾는 방식은 구현할 때 수월해서 자주 사용할 수 있을 것 같다.
- 다만 값이 어긋나면 잘못된 스탯을 참조할 수 있으므로, 추후 직접 구현할 때는 enum 값 관리에 주의해야 한다.
- 단순히 동작하게 만드는 것뿐 아니라, 어떤 클래스가 어떤 책임을 가져야 하는지도 같이 고민해야 한다는 점을 다시 확인했다.
### 내일 할 일
- 게임 일시정지를 구현하고, 일시정지 시 소유 중인 스킬을 보여주는 UI를 표시하는 예제를 프로젝트에 적용한다.
---
## 2026-07-03

### 오늘 목표
- 게임 일시정지 기능과 보유 스킬 목록 UI를 프로젝트에 적용한다.
- 스킬 레벨업 시 3개의 선택지를 표시하고, 선택한 스킬을 획득하거나 레벨업하는 흐름을 프로젝트에 적용한다.
### 오늘 한 일
- 게임 일시정지 기능을 추가하고 UI Button을 통해 일시정지와 재개 동작을 테스트했다.
- 일시정지 화면에서 현재 보유 중인 스킬 정보를 확인할 수 있도록 스킬 목록 UI를 구성했다.
- 스킬 목록 UI에서 아이콘, 레벨, 미획득 상태, 속성 보너스 상태를 표시하도록 구현했다.
- 기존 1번 키 입력 시 랜덤 스킬이 즉시 레벨업되던 테스트 흐름을 수정했다.
- 1번 키 입력 시 3개의 스킬 선택지를 표시하고, 클릭한 스킬을 획득하거나 레벨업하는 흐름을 적용했다.
- 선택한 스킬이 기존 LevelUp 흐름을 통해 처리되고, 보유 스킬 UI와 속성 보너스 UI가 정상 갱신되는지 확인했다.
### 배운 내용
- 일시정지 기능은 Time.timeScale 값을 변경해 게임 흐름을 멈추고 다시 재개하는 방식으로 처리할 수 있다.
- 보유 스킬 목록 UI는 SkillSystem이 관리하는 스킬 데이터를 기준으로 아이콘과 레벨 상태를 갱신하는 구조로 연결할 수 있다.
- UISelectSkill은 선택 가능한 스킬 목록을 표시하고, UISelectSkillIcon은 클릭된 스킬 정보를 SkillSystem에 전달하는 역할을 담당한다.
- 실제 스킬 레벨업 처리는 UI가 직접 수행하지 않고 SkillSystem의 LevelUp 흐름을 재사용하는 방식으로 구성했다.
- Instantiate는 GameObject만 반환하는 것이 아니라, 제네릭 오버로드를 사용할 경우 첫 번째 인자로 전달한 원본 프리팹의 타입에 맞는 객체를 반환받을 수 있다.
### 막힌 부분 
- 없음
### 해결한 방법
- 없음
### 개인 메모
- 선택 가능한 스킬이 3개 미만일 경우 처리와 더 이상 습득 할 스킬이 없을 때의 흐름처럼 추후 문제가 될 수 있는 부분을 확인했다.
- 기능 테스트에서는 문제없이 정상적으로 동작했기 때문에 바로 수정하지는 않았지만, 이후 프로젝트 마무리 단계에서 안정성을 위해 보완할 예정이다.
### 내일 할 일
- 경험치 테이블을 설계한다.
- 예제에서 사용한 플레이어 레벨업 흐름을 프로젝트에 적용한다.
---
## 2026-07-05

### 오늘 목표
- 경험치 테이블을 설계하고, 레벨업 예제 흐름을 프로젝트에 적용한다.
### 오늘 한 일
- 레벨별 필요 경험치를 관리하기 위한 LevelData ScriptableObject를 추가했다.
- 플레이어의 레벨과 경험치 UI를 갱신하기 위한 UIPlayerData 스크립트를 추가했다.
- 적이 사망했을 때 플레이어에게 경험치가 누적되도록 EnemyBase의 사망 처리 흐름을 수정했다.
- PlayerBase에서 누적 경험치를 일정량씩 현재 경험치로 반영하도록 처리했다.
- CurrentEXP 값이 변경될 때 레벨업 여부를 판정하고, 필요 경험치를 갱신하도록 연결했다.
- 레벨업이 발생하면 스킬 선택 UI가 출력되도록 SkillSystem 흐름과 연결했다.
- 레벨, 경험치, 사망 처리 흐름이 프로젝트에서 정상적으로 이어지는지 검증했다.
### 배운 내용
- 경험치 테이블은 코드에 직접 고정하기보다 ScriptableObject로 분리하면 레벨별 필요 경험치를 데이터로 관리할 수 있다.
- 적 사망 처리, 경험치 누적, 현재 경험치 반영, 레벨업 판정, 스킬 선택 UI 호출이 하나의 흐름으로 연결된다는 것을 확인했다.
- AccumulationExp를 사용하면 적 처치 시 획득한 경험치를 즉시 반영하지 않고, 일정량씩 흡수되도록 처리할 수 있다.
- Stat의 OnValueChanged 이벤트를 활용하면 경험치 값 변화에 따라 레벨업 판정과 UI 갱신을 연결할 수 있다.
- 레벨업 이후에는 현재 경험치를 차감하고, 다음 레벨에 필요한 경험치 값을 갱신해야 한다.
- 이벤트를 구독한 UI 객체는 파괴 시점에 구독을 해제해, 이후 구조가 변경되더라도 이벤트 참조가 남지 않도록 관리해야 한다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 초기 UI 갱신은 현재 스테이지 단위 초기화 구조에서는 필수 아님
- 레벨/경험치 이관, 저장/로드, UI 재생성 구조가 들어가면 초기 동기화 필요
- 이벤트 구독 해제는 현재 버그 수정이라기보다 이벤트 기반 구조의 안전한 생명주기 관리로 적용
### 내일 할 일
- 메모리 풀링 예제를 프로젝트에 적용해보고 흐름을 확인한다.
---
## 2026-07-05

### 오늘 목표
- 메모리 풀을 활용해 적 처치 시 생성되는 재화 이펙트를 관리하고, 재화 획득 흐름과 UI 갱신 흐름을 프로젝트에 적용한다.
- 예제 기반 구조를 실제 프로젝트에 적용하면서 재화 생성, 이동, 획득, 풀 반환 흐름을 확인한다.
### 오늘 한 일
- 적 처치 시 재화 획득 이펙트를 생성하는 흐름을 추가했다.
- GemCollector에서 MemoryPool을 사용해 재화 이펙트 오브젝트를 생성하고 반환하도록 구성했다.
- GemCollectEffect에서 재화 오브젝트가 UI 아이콘 방향으로 이동하며 획득되는 시각적 피드백을 구현했다.
- 재화 획득 시 PlayerBase의 GEM 값을 증가시키고, UIPlayerData에서 보유 재화 수치를 갱신하도록 연결했다.
- FadeEffect를 사용해 재화 이펙트가 이동하는 동안 서서히 사라지는 흐름을 적용했다.
- 일반 적 처치와 GlobalSkill 처치 상황에서 재화 생성, 획득, UI 갱신 흐름을 테스트했다.
### 배운 내용
- 메모리 풀은 오브젝트를 새로 생성하고 제거하는 대신, 미리 생성한 오브젝트를 활성화/비활성화하며 재사용하는 구조다.
- 풀링 오브젝트의 상태값과 실제 GameObject 활성 상태가 일치해야 안정적으로 재사용할 수 있다.
- IsActive 프로퍼티처럼 setter 내부에 GameObject.SetActive() 호출이 포함된 경우, 필드를 직접 수정하면 실제 오브젝트 활성 상태가 변경되지 않는다.
- 풀로 반환된 오브젝트가 실제로 비활성화되지 않으면 Update가 계속 호출될 수 있고, 같은 획득 처리가 반복 실행될 수 있다.
- ActiveCount는 현재 활성화된 풀 오브젝트 수를 추적하는 값이므로, 오브젝트의 실제 활성 상태와 어긋나면 풀 확장과 재사용 흐름이 꼬일 수 있다.
- GlobalSkill처럼 여러 적을 동시에 처치하는 흐름에서는 SpawnGemEffect() 호출이 짧은 시간에 몰리기 때문에, 풀 상태 관리 문제가 더 빠르게 드러날 수 있다.
- Screen Space - Overlay Canvas의 UI 위치를 RectTransformUtility로 변환할 때는 카메라 참조가 필요한 상황과 null을 넘겨야 하는 상황을 구분해야 한다.
### 막힌 부분
- 재화 획득 후 AddGem(), UpdateGEM() 호출 시 Invalid worldAABB 에러가 발생했다.
- GlobalSkill로 적을 처치했을 때 GemCollectEffect.Setup() 호출 흐름에서 NullReferenceException이 발생했다.
- Inspector 참조는 정상적으로 연결되어 있었지만, 재화 오브젝트의 생성과 반환 흐름 중 어떤 부분에서 문제가 발생하는지 바로 파악하기 어려웠다.
### 해결한 방법
- MemoryPool.DeactivatePoolItem()에서 풀 오브젝트를 반환할 때 IsActive 프로퍼티가 아니라 isActive 필드를 직접 수정하고 있던 문제를 확인했다.
- isActive 필드를 직접 수정하면 GameObject.SetActive(false)가 호출되지 않아 재화 오브젝트가 실제로 비활성화되지 않는 문제가 있었다.
- 비활성화되지 않은 재화 오브젝트의 Update가 계속 실행되면서 OnGemCollect()가 반복 호출되었고, 이로 인해 재화 수치가 비정상적으로 증가했다.
- 활성 상태의 오브젝트가 tempPosition인 float.MaxValue 위치로 이동하면서 Renderer bounds 계산 과정에서 Invalid worldAABB 에러가 발생한 것으로 확인했다.
- DeactivatePoolItem()에서 poolItem.IsActive = false를 사용하도록 수정해 풀 반환 시 GameObject가 정상적으로 비활성화되도록 했다.
- 수정 후 일반 적 처치, GlobalSkill 처치, 재화 획득, UI 갱신 흐름을 다시 테스트했고 이상 없이 동작하는 것을 확인했다.
### 개인 메모
- MemoryPool.cs의 DestroyObjects()에서 Destroy() 후 poolItemList를 Clear()할 예정이라면 poolItemList[i].isActive = false 처리는 생략해도 될 것으로 보인다. DestroyObjects()는 재사용을 위한 비활성화가 아니라 풀 오브젝트를 제거하고 리스트를 초기화하는 흐름이기 때문에, 이후 다시 사용하지 않을 상태값을 갱신할 필요는 크지 않다.
- GemCollectEffect.cs의 UpdateEndPoint()는 UI 아이콘 위치를 화면 좌표로 변환한 뒤, 다시 RectTransform 기준의 월드 좌표로 변환해 재화 오브젝트의 도착 지점을 갱신하는 흐름이다.
- RectTransformUtility.WorldToScreenPoint(Camera cam, Vector3 worldPoint)의 첫 번째 매개변수는 Canvas의 Render Mode에 따라 다르게 설정해야 한다. Screen Space - Overlay는 카메라를 사용하지 않고 화면에 직접 렌더링되므로 null을 전달하고, Screen Space - Camera나 World Space에서는 Canvas에 등록된 카메라 또는 해당 UI를 바라보는 카메라를 전달한다.
- 이때 null은 MainCamera를 자동으로 할당한다는 의미가 아니라, Screen Space - Overlay에서는 카메라가 필요 없기 때문에 사용하는 값으로 이해하는 것이 맞다.
### 내일 할 일
- NavMesh2D를 활용하여 장애물 회피와 길 찾는 예제를 학습한다.
---
## 2026-07-06

### 오늘 목표
- 행동 트리와 스마트 경로 탐색 예제를 학습한다.
- NavMesh2D를 활용한 장애물 회피와 길찾기 설정 흐름을 확인한다.
- 행동 트리를 기반으로 적 AI의 상태 판단과 전이 흐름을 파악한다.
### 오늘 한 일
- 행동 트리와 스마트 경로 탐색 예제를 학습했다.
- NavMeshAgent를 활용해 적이 배회, 추적 행동을 수행하는 흐름을 확인했다.
- BehaviorGraphAgent와 Blackboard 변수를 통해 AI 행동에 필요한 값을 전달하는 구조를 확인했다.
- Action 노드와 Condition 노드를 분리해 거리 갱신, 타겟 감지, 추적, 공격 요청을 처리하는 흐름을 확인했다.
- 예제 실습을 통해 적의 배회, 추적, 공격, 장애물 회피 동작이 정상적으로 동작하는 것을 확인했다.
### 배운 내용
- 행동 트리는 AI의 판단 조건과 실행 행동을 노드 단위로 분리해 구성할 수 있다.
- Blackboard 변수는 행동 트리의 여러 노드가 공유하는 데이터 저장소처럼 사용된다.
- UpdateDistanceAction에서 계산한 거리 값을 CurrentDistance에 저장하고, CheckTargetDetectCondition에서 해당 값을 기준으로 타겟 감지 여부를 판단하는 흐름을 확인했다.
- ChaseAction에서는 NavMeshAgent의 목적지를 타겟 위치로 설정해 추적 행동을 수행하는 흐름을 확인했다.
- WeaponAction은 공격 실행을 요청하고, 실제 공격 가능 여부와 쿨타임 관리는 WeaponBase에서 처리하는 구조를 확인했다.
- 2D 환경에서 NavMeshAgent를 사용할 때 updateRotation과 updateUpAxis를 비활성화해 불필요한 3D 회전 처리를 막을 수 있다는 점을 확인했다.
### 막힌 부분
- BT Graph를 통해 행동 분기를 만들고 설계하는 작업이 처음이라 구조가 낯설었고, 노드 흐름을 이해하는 과정에서 조금 헤맸다.
### 해결한 방법
- 예제 실습을 따라가며 Blackboard 변수, Action 노드, Condition 노드가 어떤 순서로 연결되는지 확인했다.
- 적 AI가 배회, 추적, 공격 상태로 전이되는 흐름을 직접 실행해보며 정상 동작을 확인했다.
### 개인 메모
- 행동 트리 구조가 편리하고 유용하다고 느꼈다.
- 아직 BT Graph 기반 설계 방식이 익숙하지 않기 때문에, 추후 프로젝트에 적용해보면서 더 익숙해질 필요가 있다.
### 내일 할 일
- 비동기 씬 로딩과 로비를 구성하는 방법에 대해 학습한다.
---
## 2026-07-07

### 오늘 목표
- AsyncOperation을 활용한 비동기 씬 로딩 시스템 구현 방법 학습한다.
- 스와이프 기반 UI를 설계하여 모바일 친화적인 인터페이스를 구현하는 방법 학습한다.
### 오늘 한 일
- SceneLoader를 통해 공통 씬 로딩 시스템을 구성하는 예제 흐름을 학습했다.
- AsyncOperation을 사용해 씬을 비동기로 로드하고, 로딩 진행도를 Slider와 Text UI에 반영하는 흐름을 확인했다.
- IntroSceneController에서 인트로 텍스트 Fade 효과를 반복 재생하고, 입력이 감지되면 다음 씬으로 이동하는 흐름을 확인했다.
- Utils에 IsAnyInputDown()을 추가해 키보드, 마우스, 터치 입력을 공통으로 감지하는 구조를 확인했다.
- ChapterData를 ScriptableObject로 구성하고, 챕터 해금 여부, 최고 스테이지, 챕터 표시 정보, 스테이지 구성 정보를 나누어 관리하는 흐름을 학습했다.
- LobbySceneController에서 ChapterData 배열을 기반으로 챕터 아이콘 UI를 동적으로 생성하는 흐름을 확인했다.
- UIChapterIcon에서 챕터 잠금 상태, 챕터 이미지, 챕터 이름, 최고 스테이지 정보를 UI에 반영하는 구조를 확인했다.
- SwipeUI에서 Scrollbar value를 페이지 단위로 나누고, 입력 시작 위치와 종료 위치를 비교해 스와이프 방향을 판정하는 흐름을 학습했다.
- 스와이프 입력 거리가 기준값보다 짧으면 현재 페이지를 유지하고, 유효한 스와이프일 경우 현재 페이지를 변경한 뒤 Lerp로 부드럽게 이동하는 구조를 확인했다.
- 인트로 입력, 로비 이동, 챕터 스와이프, 챕터 시작, 게임 씬 이동까지 정상 동작을 확인했다.
### 배운 내용
- AsyncOperation을 사용하면 씬 로딩을 비동기로 처리하면서 로딩 진행 상황을 UI에 반영할 수 있다.
- DontDestroyOnLoad를 사용하면 씬이 전환되어도 공통 로딩 시스템을 유지할 수 있다.
- 씬 이름을 enum으로 관리하면 문자열 직접 입력을 줄이고, 씬 전환 호출부의 의도를 명확하게 만들 수 있다.
- ScriptableObject를 사용하면 챕터 표시 데이터와 스테이지 구성 데이터를 에셋 단위로 관리할 수 있다.
- Scrollbar value를 페이지별 위치 값으로 미리 계산하면 페이지 단위의 스와이프 UI를 구현할 수 있다.
- 입력 시작 위치와 종료 위치의 X 좌표 차이를 비교하면 스와이프 방향과 유효 거리를 판정할 수 있다.
- 스와이프 이동 중 추가 입력을 막으면 페이지 이동 중 중복 입력으로 인한 상태 꼬임을 줄일 수 있다.
- 키보드, 마우스, 터치 입력을 공통 유틸 함수로 분리하면 인트로 화면처럼 입력 장치에 상관없이 같은 동작을 처리해야 하는 곳에서 재사용하기 좋다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 오늘 학습한 내용은 프로젝트에 적용할 때 한 번 더 검증이 필요해 보인다.
### 내일 할 일
- 스테이지 클리어 로직과 챕터 진행 시스템을 만드는 방법 학습한다.
- JSON 직렬화를 통해 게임 세이브/로드를 구현하는 방법 학습한다.
---
## 2026-07-09

### 오늘 목표
- 스테이지 클리어 로직과 다음 스테이지로 진행되는 흐름 학습한다.
### 오늘 한 일
- 스테이지 시작 시 적이 생성될 위치를 표시하기 위한 SpawnTile 클래스를 추가했다.
- EnemySpawner에서 SpawnEnemys()와 Process()를 통해 적 생성 위치를 먼저 표시한 뒤, 일정 시간 후 실제 적을 생성하는 흐름을 확인했다.
- GameController에서 현재 챕터 정보를 바탕으로 최대 스테이지를 설정하고, 스테이지가 진행될 때마다 적을 생성하는 흐름을 확인했다.
- 모든 적이 제거되면 EnemySpawner의 exitEvent를 통해 GameController의 SetupStage()가 다시 호출되고, 다음 스테이지로 넘어가는 구조를 확인했다.
- LobbySceneController에서 게임 시작 시 선택한 챕터 인덱스를 PlayerPrefs에 저장하고, GameController에서 해당 값을 읽어오는 흐름을 확인했다.
- PlayerPrefs에서 사용할 키 값을 Constants 정적 클래스에 분리하는 방식을 확인했다.
### 배운 내용
- 스테이지 클리어 판정은 남은 적 수를 기준으로 처리할 수 있으며, 적 리스트가 비었을 때 이벤트를 호출해 다음 스테이지 설정으로 연결할 수 있다.
- 스폰 위치를 먼저 시각적으로 보여준 뒤 실제 적을 생성하면, 플레이어가 적 등장 위치를 미리 인지할 수 있다.
- PlayerPrefs는 간단한 값 저장에 사용할 수 있으며, 키 값을 Constants 같은 정적 클래스로 분리하면 문자열 하드코딩을 줄일 수 있다.
- enemyCountScale 값은 스테이지별 적 수 증가량으로 사용되며, int 캐스팅으로 소수점이 버려지기 때문에 일정 스테이지 간격으로 적 수가 증가한다.
### 막힌 부분
- 없음. 
### 해결한 방법
- 없음. 
### 개인 메모
- 없음. 
### 내일 할 일
JSON 직력화를 통해 게임 데이터를 저장하고 불러오는 방법을 학습한다.
컨트롤러를 추가하고 빌드하는 방법을 학습한다.
---
## 2026-07-09

### 오늘 목표
- JSON을 활용한 데이터 저장/로드 흐름을 학습하고, 컨트롤러 추가 및 빌드 준비 흐름을 확인한다.
### 오늘 한 일
- 예제 기반으로 JSON 저장/로드 시스템을 학습했다.
- Database 클래스를 통해 플레이어, 재화, 챕터 진행 데이터를 하나의 DatabaseItem으로 묶어 저장하고 불러오는 흐름을 확인했다.
- 저장 데이터가 없을 때 초기 데이터를 생성하고, 첫 번째 챕터만 잠금 해제 상태로 설정하는 흐름을 확인했다.
- 로비에서 저장된 플레이어 레벨, 경험치, 보석, 챕터 잠금 해제 여부, 최고 도달 스테이지를 UI에 표시하는 흐름을 확인했다.
- 로비에서 선택한 챕터의 잠금 해제 여부와 하트 보유량을 검사한 뒤 게임 씬으로 진입하는 흐름을 확인했다.
- 게임 클리어와 게임 오버 시 보상, 최고 기록, 챕터 해금 정보를 저장하고 결과 UI에 출력하는 흐름을 확인했다.
- 하트 소모, 충전 타이머, 앱 종료 후 경과 시간에 따른 하트 회복 흐름을 확인했다.
- 에디터에서 전처리 분기를 주석 처리한 뒤 가상 조이스틱 입력이 정상 동작하는지 확인했다.
### 배운 내용
- JSON 저장 방식은 플레이어 정보, 재화, 챕터 진행도처럼 여러 데이터를 하나의 구조로 묶어 저장할 때 유용하다는 점을 확인했다.
- Database.Read()는 저장 파일이 있으면 데이터를 불러오고, 파일이 없으면 Reset()을 통해 초기 데이터를 생성한 뒤 저장하는 흐름으로 구성된다는 점을 확인했다.
- 게임 결과 화면에서 보상과 진행 정보를 계산한 뒤 Database.DBItem에 반영하고, Database.Write()를 호출해 파일에 저장하는 흐름을 확인했다.
- 하트 시스템은 현재 하트 수와 충전 타이머를 저장하고, 다시 실행했을 때 이전 저장 시각과 현재 시각의 차이를 계산해 오프라인 충전을 반영한다는 점을 확인했다.
- 에디터와 Android 환경에서 입력 처리 방식을 다르게 구성할 수 있으며, 전처리 지시문을 통해 플랫폼별 입력 흐름을 분기할 수 있다는 점을 확인했다.
- 로비에서 레벨 계산은 1회 호출되지만, 누적 경험치가 여러 레벨업 조건을 만족할 수 있으므로 반복문 내부에서 현재 레벨 기준값도 함께 갱신해야 안전하다는 점을 확인했다.
- 로비에서 레벨업 처리 시 저장 데이터의 레벨 값만 증가시키면 지역 변수로 보관한 레벨 값은 바뀌지 않으므로, 다음 반복에서 잘못된 경험치 테이블을 참조할 수 있다는 점을 확인했다.
### 막힌 부분
- 전체적으로 저장/로드, 로비 UI, 챕터 진행, 하트 시스템, 컨트롤러 입력 흐름이 한 번에 연결되어 있어 코드 흐름을 읽는 데 익숙하지 않았다.
### 해결한 방법
- 저장 데이터가 어떤 클래스에 들어가고, 어느 시점에 읽고 쓰이는지 천천히 따라가며 흐름을 파악했다.
- 프로젝트에 적용할 때도 한 번에 전체 구조를 옮기기보다 저장/로드, 로비 UI, 챕터 진행, 하트 시스템 순서로 나누어 확인하기로 했다.
### 개인 메모
- 프로젝트에 적용할 때 흐름 파악에 집중하고, 저장 데이터와 UI, 게임 결과가 연결되는 구조에 익숙해질 필요가 있어보인다.
### 내일 할 일
- NavMesh2D를 활용한 길 찾기를 프로젝트에 적용
- BT를 이용한 적 상태 전환을 프로젝트에 적용
---
## 2026-07-10

### 오늘 목표
- NavMesh를 활용하여 적 장애물 회피, 이동을 적용한다.
### 오늘 한 일
- 적 오브젝트에 NavMeshAgent를 추가했다.
- 2D 환경에서 사용할 수 있도록 회전과 Up Axis 자동 갱신을 비활성화했다.
- 게임 시작 시 플레이어의 위치를 목적지로 지정하여 적이 NavMesh 경로를 따라 이동하도록 임시 적용했다.
### 배운 내용
- Navigation Surface와 NavMeshCollectSources2d를 활용해 Tilemap, Sprite, Collider2D 등의 정보를 수집하고, 적이 이동할 수 있는 NavMesh 경로 데이터를 생성하는 흐름을 확인했다.
- 기본 설정과 다르게 처리할 오브젝트에는 Navigation Modifier 컴포넌트를 추가하고, Override Area를 활성화해 Walkable, Not Walkable 등의 Area Type을 지정할 수 있다는 것을 확인했다.
- 오브젝트와 Navigation 설정을 마친 뒤 Navigation Surface에서 Bake를 실행하면 현재 맵 구조를 기준으로 NavMesh 데이터가 생성된다는 것을 확인했다.
- 맵의 지형, 충돌 영역 또는 Navigation Modifier 설정이 변경되면 변경된 구조가 반영되도록 NavMesh를 다시 Bake해야 한다는 것을 확인했다.
- NavMeshAgent가 Transform의 방향을 자동으로 갱신하지 않도록 updateRotation을 비활성화하고, 2D XY 평면에서 에이전트가 NavMesh의 Up Axis에 맞춰 정렬되지 않도록 updateUpAxis도 비활성화해야 한다는 것을 확인했다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 없음.
### 내일 할 일
- 행동 트리를 활용해 적의 상태를 유기적으로 제어하는 구조를 프로젝트에 적용한다.
---
## 2026-07-11

### 오늘 목표
- 행동 트리를 활용해 적의 상태와 행동을 제어하는 구조를 프로젝트에 적용하고 전투 흐름을 확인한다.
### 오늘 한 일
- Behavior Graph를 활용해 적의 Idle, Patrol, Wander, Chase, Attack 행동 흐름을 구성했다.
- 적의 행동 분기에 사용할 EnemyState를 Blackboard Enum으로 추가했다.
- 적과 플레이어 사이의 거리를 계산하고 Blackboard의 CurrentDistance를 갱신하는 UpdateDistanceAction을 추가했다.
- CurrentDistance와 ChaseDistance를 비교해 플레이어 감지 여부를 판단하는 CheckTargetDetectCondition을 추가했다.
- NavMeshAgent를 활용해 무작위 위치로 이동하는 WanderAction과 플레이어를 추적하는 ChaseAction을 추가했다.
- Patrol 행동은 Behavior Graph에서 제공하는 기본 Navigate 노드를 활용해 구성했다.
- 기존 WeaponBase의 TryAttack()을 호출하는 WeaponAction을 추가해 Behavior Graph의 Attack 행동과 기존 공격 시스템을 연결했다.
- EnemyFSM에서 BehaviorGraphAgent를 참조하고 적 생성 시 Target과 PatrolPoints를 Blackboard에 전달하도록 수정했다.
- EnemySpawner에서 적마다 사용할 WayPoint 그룹을 무작위로 선택해 전달하도록 수정했다.
- Behavior Graph를 통한 상태 전환과 NavMeshAgent 이동, 공격 흐름이 정상적으로 동작하는 것을 확인했다.
### 배운 내용
- Behavior Graph의 Blackboard를 통해 Target, PatrolPoints, CurrentDistance와 같은 데이터를 여러 행동 노드에서 공유하는 흐름을 확인했다.
- Action 노드는 실제 행동을 처리하고 Condition은 행동을 실행할 수 있는 조건을 판단하도록 역할을 분리할 수 있다는 것을 확인했다.
- Try In Order는 앞쪽 브랜치부터 순서대로 확인하며, 현재 브랜치가 Failure를 반환하면 다음 브랜치를 실행하고 Success를 반환하면 이후 브랜치를 확인하지 않는 구조라는 것을 이해했다.
- 실행 중인 노드가 Running 상태이면 해당 행동을 계속 실행하며, 조건 변화로 현재 행동이 중단되면 행동 흐름을 다시 평가해 다른 상태로 전환할 수 있다는 것을 확인했다.
- 기존 FSM 방식보다 Behavior Graph를 활용한 방식이 여러 행동과 전환 조건을 시각적으로 구성하기 편리하다고 느꼈다.
- Try In Order의 실행 흐름이 처음에는 익숙하지 않았지만 실제 프로젝트의 행동 분기를 구성하면서 이전보다 흐름을 이해할 수 있었다.
### 막힌 부분
- 게임 실행 시 `Failed to get BlackboardReference. Agent is not yet initialized.` 경고가 발생했다.
- 디버깅 모드에 진입했다가 나온 이후에는 동일한 경고가 다시 발생하지 않아 정확한 발생 원인은 확인하지 못했다.
### 해결한 방법
- 현재 동일한 경고가 재현되지 않아 별도의 코드 수정은 진행하지 않았으며, 추후 다시 발생할 경우 BehaviorGraphAgent의 초기화 시점과 Blackboard 변수 설정 시점을 확인할 필요가 있다.
### 개인 메모
- Try In Order는 앞쪽 브랜치부터 순서대로 실행 가능한 행동을 찾고, Failure이면 다음 브랜치로 넘어가며 Success이면 해당 흐름을 종료한다. Running 상태에서는 현재 행동을 계속 실행한다.
- Blackboard는 Target, 거리, 상태와 같이 여러 노드에서 필요한 데이터를 공유하는 역할을 하고, Action과 Condition을 조합해 행동과 전환 조건을 분리할 수 있다.
- Behavior Graph는 실행 중인 행동과 조건 변화에 따라 그래프를 반복해서 평가하며 적절한 행동으로 전환한다.
- FSM보다 행동을 제어하는 과정은 간단하게 느껴졌지만 Graph 구성과 각 노드의 실행 흐름에는 아직 익숙해질 필요가 있다.
### 내일 할 일
- 비동기 씬 로드 기능을 프로젝트에 적용한다.
- Swipe UI를 활용한 챕터 선택 기능을 프로젝트에 적용한다.
- 로비 구성 작업을 진행한다.
---
## 2026-07-13

### 오늘 목표
- 인트로, 로비, 게임 씬으로 이어지는 비동기 씬 전환 흐름을 프로젝트에 적용한다.
- 챕터 데이터를 기반으로 로비의 챕터 선택 UI를 구성한다.
### 오늘 한 일
- FadeEffect의 Fade()가 TextMeshProUGUI를 매개변수로 받을 수 있도록 메서드 오버로딩을 추가했다.
- Utils에 키보드, 마우스, 모바일 터치 입력을 통합해서 확인하는 IsAnyInputDown() 정적 메서드를 추가했다.
- 인트로 안내 문구가 반복해서 페이드 인·아웃되도록 구성하고, 임의 입력이 발생하면 다음 씬으로 이동하도록 구현했다.
- 싱글턴과 DontDestroyOnLoad()를 활용해 씬 전환 후에도 유지되는 SceneLoader를 구성했다.
- LoadSceneAsync()와 AsyncOperation을 활용해 씬을 비동기로 불러오고, 로딩 진행도를 슬라이더와 퍼센트 텍스트에 표시했다.
- 로딩 화면에 무작위 배경 이미지가 출력되도록 구성했다.
- ScriptableObject 기반 ChapterData를 생성해 챕터 진행 정보, UI 표시 정보, 스테이지 설정 데이터를 관리하도록 구성했다.
- 챕터 데이터를 기반으로 로비의 챕터 아이콘을 동적으로 생성하고, 잠금 상태와 스테이지 진행도를 표시했다.
- 스크롤바의 위치값을 활용해 챕터 페이지를 한 칸씩 이동하는 스와이프 기능을 구현했다.
- 현재 선택한 챕터의 해금 여부를 확인한 뒤 게임 씬으로 이동하도록 구성했다.
### 배운 내용
- 공통 씬 로더를 별도로 두면 각 씬의 컨트롤러는 전환 대상만 전달하고, 비동기 로딩과 로딩 UI 처리는 SceneLoader에서 일관되게 관리할 수 있다.
- AsyncOperation의 진행 상태를 UI에 연결해 씬 로딩 과정을 사용자에게 표시할 수 있다.
- 입력 장치별 판정을 유틸리티 메서드로 분리하면 인트로 컨트롤러가 구체적인 키보드, 마우스, 터치 처리 방식에 직접 의존하지 않는다.
- ScriptableObject를 활용하면 챕터별 표시 정보와 스테이지 설정 데이터를 에셋 단위로 관리하고 UI 생성에 재사용할 수 있다.
- 스와이프 UI는 페이지별 스크롤바 위치값과 현재 페이지 인덱스를 함께 관리해 한 페이지 단위의 이동을 제어할 수 있다.
- 전처리 지시문을 활용하면 에디터의 마우스 입력과 Android의 터치 입력 코드를 플랫폼별로 분리할 수 있다.
### 막힌 부분
- 없음.
### 해결한 방법
- 없음.
### 개인 메모
- 없음.
### 내일 할 일
- 스테이지 클리어 로직과 JSON 직렬화를 활용한 저장/로드를 프로젝트에 적용한다.
---